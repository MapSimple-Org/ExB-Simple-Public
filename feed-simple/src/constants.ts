/**
 * Shared constants for the FeedSimple widget.
 */

/** Viewport width breakpoint (px) at which mobile layout activates */
export const MOBILE_BREAKPOINT_PX = 600

/** Duration (ms) for map goTo animations */
export const MAP_ANIMATION_DURATION_MS = 800
