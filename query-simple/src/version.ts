/**
 * Widget version number.
 * Base version matches Experience Builder version (1.20.0).
 * Release number increments on major changes (001, 002, 003, ...).
 * Minor version increments on smaller fixes between major releases (r015.1, r015.2, ...).
 * 
 * To increment:
 * - Major change: Update RELEASE_NUMBER (e.g., 015 -> 016) and reset MINOR_VERSION to 0
 * - Minor fix: Increment MINOR_VERSION (e.g., 1 -> 2) while keeping same RELEASE_NUMBER
 */
const BASE_VERSION = '1.20.0'
const RELEASE_NUMBER = '028' // Increment on major changes (001, 002, 003, ...)
const MINOR_VERSION = '157' // r028.157 TODO #44: View-in-Table now REVEALS the Table widget (bug fix by ruling - the behavior users always expected; NO config toggle, r028.137 no-config precedent). New runtime/table-reveal-utils.ts: resolveRevealSteps (PURE - appConfig in, steps out; walks widget/section/view layout maps per size mode; sidebar step ONLY when the chain rides the sidebar's collapseSide (CRR P9 - app 7's toolbox/nav sidebars are the live exclusion fixture); controller-open and section-nav (r028.140 aria-controls click) legs for future placements; never throws, honest empty on unknown shapes) + executeRevealSteps (the framework's own dispatches; every step and outcome logged as table-reveal under VIEW-TABLE) + revealTableWidget convenience. Wired into BOTH handler paths (new-tab after data delivery - ordering gate-verified in both apps r028.156 - and the reused-tab early return, which the probe never covered, P2). probeRevealTable REMOVED entirely. LOGGED DECISION (deviation from the brief's searchUtils lean, reason recorded): the resolver hand-walks the config rather than importing jimu-layouts searchUtils - the pure-module contract (plain-JSON Jest fixtures, cold-room authored) cannot be met by a framework util bound to Immutable app state; seamless-immutable reads make one walk serve both. Probe union-walk logs from apps 1/7 are the validation baseline. Cold-room tests authored separately from the brief (table-reveal-utils.test.ts) - run unmodified per protocol. Manual pass pending: both apps, repeat-click (P10), reused-tab path. query-simple only, no shared-code, no FS bump. PRIOR r028.156 TODO #44 FEASIBILITY PROBE (throwaway, gate before the real build - Adam: 'hacky test to see if this will even work... before spending a bunch of time'; the placeholder saga is why we gate now). probeRevealTable() in view-in-table-action.tsx: after View-in-Table's data delivery, walks the app-config widget tree up from the Table widget, finds every Sidebar ancestor (all size-mode twins), dispatches widgetStatePropChange(id, 'collapse', true) (= EXPAND per sidebar widget semantics), logs chain + before/after state under the existing VIEW-TABLE tag (?debug=VIEW-TABLE). Sidebar leg only, no config, no controller/section legs - those are the real build per docs/specs/TABLE_REVEAL_BRIEF.md. PASS = table pane visibly opens on View-in-Table in apps 1/7 + probe-reveal logs dispatched with expected chain. FAIL evidence goes back into the brief before any build. Probe is removed/replaced by the spec'd walker in the real build. query-simple only. PRIOR r028.155 Placeholder pin, closed by console evidence from Adam ({tokenOnHost: NOT DELIVERED, sysVarResolves: VARIABLE UNDEFINED, placeholder rgb(95,94,94)}): DOUBLE failure - the .154 wrapper-scoped emotion rule never reached the combobox host (cause not diagnosed; sidestepped), AND --sys-color-text-primary is undefined in this app's theme, so every attempt since .151 carried a dead value. Elsewhere in the widget that var 'works' in 11 usages only because normal text falls back to inherited dark - logged as TODO #43, not fixed here (scope containment). Fix: inline style on the calcite-combobox host - the house-proven delivery route (truncation popover precedent, query-result.tsx ~1293) - setting --calcite-input-placeholder-text-color: currentColor, so the placeholder renders in the control's own typed-text color. No theme variable dependency at all; survives theme swaps and the undefined-var landscape. The .154 wrapper rule removed, not layered over. query-simple only. PRIOR r028.154 Placeholder pin, evidence-based (research agent, live-verified against the RUNNING calcite build). Root cause of the .153 injection failure PROVEN by live repro: ExB loads calcite from the js.arcgis.com 5.0.2 CDN ESM build (SystemJS hook in jimu-core/init.js; webpack.common.js:49), which registers LAZY PROXY custom elements at boot - customElements.whenDefined resolves before the implementation chunk hydrates and attachShadow runs, so the .153 effect found shadowRoot null and its one-shot guard bailed silently. Also proven: the placeholder IS a plain native input one open shadow root deep, and BOTH earlier token approaches recolor it against the real build - the r028.152 dedicated token was almost certainly correct but never runtime-tested (that round's verdict screenshot showed a r028.151 footer; stale-bundle test loop, process lesson recorded). Fix: .153 injection REMOVED (dead machinery, not layered over); --calcite-input-placeholder-text-color: var(--sys-color-text-primary) declared via a calcite-combobox child rule on OUR wrapper div (plain-div emotion delivery, inherits through the shadow boundary; calcite PR #11972 token, undocumented on the combobox doc page - re-verify on calcite upgrades). If still gray after rebuild, the one devtools check is whether the property appears in the combobox host's computed styles. query-simple only. PRIOR r028.153 Placeholder pin, third and decisive approach: neither calcite token override took in the running build (.151 global text-3, .152 the dedicated --calcite-input-placeholder-text-color read from node_modules calcite 5.0.2 - the running component may be a different embedded build, unconfirmed). Outside CSS cannot reach ::placeholder across shadow DOM at all, so per Adam's 'just override the CSS' the widget now injects ONE <style id=qs-placeholder-pin> into the combobox's OPEN shadow root via the existing spatialRelComboboxRef: ::placeholder { color: var(--sys-color-text-primary) !important; opacity: 1 !important }. Token-name-proof; jimu --sys vars inherit through the boundary; customElements.whenDefined guards the upgrade race; idempotent by id check. Decoration-only DOM addition, lighter than the typeahead's existing event-dispatch precedent; remove if a future calcite exposes a working token. query-simple only. PRIOR r028.152 Fixes the r028.151 placeholder pin, which did not take (Adam's screenshot at .151 still showed the light placeholder). Cause read from the calcite-combobox stylesheet in node_modules: placeholder color is var(--calcite-input-placeholder-text-color, var(--calcite-color-text-3)), and overriding the text-3 FALLBACK from outside fails because calcite components redefine the global --calcite-color-* tokens inside their own shadow scopes; the dedicated component token is consumed but never redefined, so it inherits from the host. Swapped the override to --calcite-input-placeholder-text-color: var(--sys-color-text-primary). Also removes the .151 chevron-tint caveat - this token touches only the placeholder. query-simple only. PRIOR r028.151 Spatial relationship selector: native disabled gating removed (Adam's ruling; DCE-batch consistency one level down). The combobox was disabled={!operationsEnabled} until the mode had input, so a REQUIRED selector rendered washed-out and read as inert while Target layers (never gated) read act-on-me - the inconsistency Adam flagged. Now always enabled: pre-selecting a relationship before drawing/searching is a valid order, canExecute still gates execution, the existing mode hints stay, and the refusal popover names what is missing. Placeholder pinned to var(--sys-color-text-primary) via --calcite-color-text-3 so both selectors' empty-state text carries the same do-something weight (note: text-3 may also tint the combobox chevron - visually acceptable, flag if not). Pre-existing and untouched: the buffer unit select's own disabled gating (out of scope). query-simple only. PRIOR r028.150 Query-tab REQUIRED marker moved inline with the clause label (Adam's walkthrough: r028.149 put it on the form-title row, but items with no configured title left the marker floating on its own line). Now a ::after on .sql-expression-single:first-of-type .sql-expression-label, scoped inside our sqlExprRuntimeContainerRef css - the same compose-around boundary the typeahead uses; SqlExpressionRuntime itself untouched. Reads 'Search for full address REQUIRED' / 'Parcel number is REQUIRED' regardless of title config. First clause only (validity = at least one clause filled; marking all clauses would overstate). Marker text i18n (qsRequiredMarker, sanitized for CSS content). required-marker-style.ts now exports raw requiredMarkDeclarations shared by the Spatial element markers and this pseudo-element - one source, no drift. Esri DOM classname dependency: sql-expression-single/.sql-expression-label (verified in 1.20 dist) - RE-VERIFY ON EXB UPGRADES; if the classes change the marker silently disappears, refusal popover still covers. query-simple only. PRIOR r028.149 REQUIRED marker on the Query tab (user request during the DCE walkthrough: with the Spatial headers marked, the Query tab read as missing). Marker renders on the form-title row (our markup; the clause label inside SqlExpressionRuntime is Esri's - composed around, not modified), shown when !isInputValid, which is exactly the required-and-unmet condition: list/dropdown clauses and spatial-only queries are exempt inside isInputValid, and the instant-typing path clears the marker on the first character. requiredMarkStyle promoted from a SpatialTabContent local to shared required-marker-style.ts (one source of truth, both tabs). Known boundary: a query item configured with no filter label AND no description keeps its d-none title row, so no marker renders there - the refusal popover still covers it. query-simple only. PRIOR r028.148 Second i18n shadow, same class as r028.146: the spatial Required marker rendered jimu-core's OWN requiredField string ("This is a required field." in en, localized in every locale) instead of our terse 'Required' - the framework key shadowed ours by id, uppercased by our marker CSS (found on Adam's manual pass; he was reviewing UX of a sentence we never wrote). Key renamed requiredField -> qsRequiredMarker, both header usages updated. Swept ALL r028.143+ keys against dist/jimu-core + dist/jimu-ui: searchButtonLabel and the six blocked-message keys are clean; this was the last collision. Marker now reads REQUIRED (terse, per the UX layering: scan-marker / actionable refusal / tab help). query-simple only. PRIOR r028.147 Collapsed-card dropdown z-order fix (pre-existing, r024.46-era; surfaced by Adam's DCE-batch walkthrough, NOT introduced by the batch). The open three-dot menu painted UNDER sibling cards' toolbar buttons: .result-actions-menu (z-index 10) is a stacking context, so the popup's z-index 1000 flattens to 10 outside it, and sibling toolbars at the same 10 win by later DOM order. Fix: style={menuOpen ? { zIndex: 20 } : undefined} on the open card's toolbar container - lifts the whole context above the siblings only while its menu is open. Visual-only, no automated coverage on this component, verify manually (open a middle card's menu over the cards below). query-simple only. PRIOR r028.146 Item 2 rename actually lands: the button still showed 'Apply' after r028.141 because jimu-core ships an 'apply' i18n key in EVERY locale (dist/jimu-core, e.g. apply:"Apply" in en) and react-intl resolves ids from the app locale's IntlProvider BEFORE widget defaultMessages - the widget's default.ts value only fills ids the framework lacks, so our colliding key was silently shadowed (found on Adam's manual pass; every other batch string worked because those keys are widget-unique). Fix: widget-unique key searchButtonLabel: 'Search' replacing apply:, both consumers updated (query-task-form Search button, SpatialTabContent execute button). LESSON for future keys: never name a widget i18n key the same as a common framework string (apply, ok, cancel, delete...); the shadow is silent and locale-dependent. Note 'reset' collides the same way but both values are 'Reset' so it is invisible - left alone (scope containment). query-simple only. PRIOR r028.145 Follow-up to r028.142 (TODO #42) from Adam's manual pass: on 3-icon cards the inner feature-info box (own 1px border, own 4.5rem floor) stayed 72px while the card grew to 108px, leaving its bottom border as a stray short line mid-card. New descendant rule in the card style block: .result-item-expanded[data-action-count='3'] .feature-info-expanded { min-height: 6.75rem } - the inner box tracks the card in the 3-icon case only, restoring the flush border relationship. Covers BOTH content branches (standalone FeatureInfo and CustomTemplate/SelectAttributes - both class feature-info-expanded); parent-block specificity (0,3,0) outranks the inner floors (0,2,0). 1-2 icon rows keep the untouched 4.5rem inner floor by design (brief Section 3). Visual-only; no automated coverage on this component - re-verify manually. query-simple only. PRIOR r028.144 HOTFIX for r028.143: QueryTaskForm crashed on mount with 'Cannot access showSearchRefusal before initialization'. The r028.143 block (applyBlocked/showSearchRefusal/handleSearchClick/aria-disabled effect) was declared AFTER handleKeyDown, but handleKeyDown lists showSearchRefusal in its useCallback deps - the deps array evaluates at render time, so first render hit the temporal dead zone. Block moved above handleKeyDown; no logic change. Caught by Adam's manual run, NOT by the suite - nothing mounts QueryTaskForm in Jest (the exact coverage hole the CRR documented; tsc does not flag TDZ across a deps array either). Same-class sweep: resetBlocked (form) and the spatial block declare before first use - this was the only instance. query-simple only. PRIOR r028.143 DCE batch items 1+5: blocked Search explains itself (aria-disabled pattern + spatial required markers). Both tabs' Search/Reset move from native disabled to aria-disabled: focusable, SR-announced, blocked look via shared blocked-button-style.ts (gray, no hover affordance, label #595959 on #f0f0f0 ~6.2:1 - DCE's low-vision complaint). Attribute stamped imperatively by ref effect because the compiled jimu Button OVERWRITES a caller aria-disabled prop with its own disabled value (CRR E1; re-verify on ExB upgrades). Activating blocked Search shows a refusal popover (house feedback pattern, amber, per-widget anchors - NEW search-refusal-anchor-{widgetId} on the query form per CRR M3) + polite live-region announce (r028.138 announcer; onAnnounce threaded query-task -> QueryTabContent -> QueryTaskForm; spatial already had it) + FORM/TASK 'search-refused' log events. Blocked Reset is a silent no-op by ruling (P1.3). Refusal reasons from NEW PURE block-reason-utils.ts (cold-room test target): query form = datasource-loading | input-invalid; spatial = first unmet requirement in form order no-drawing/no-results -> no-relationship -> no-layers (one actionable reason at a time, P5.2; transient async geometry-assembly reports the mode's geometry reason, CRR U1). THE GUARD LIVES IN THE USER-ACTIVATION WRAPPER ONLY (P1.9/CRR M1): applyQuery() itself is untouched - the hash-conversion listener (:958) and dataActionFilter auto-trigger (:761) call it directly and must never be refused (validity state lags the ref value on those paths). Enter path reworked (P1.5/CRR E4): the outer Enter guard used to silently swallow blocked Enter; it now routes to the same refusal as a click; the dead .disabled ref checks removed (click always routes the wrapper). Spatial execute keeps its inner relIsValid safety guard; isExecuting re-entry stays silent (P1.8). Christie's case (item 5): Spatial Relationship and Target layers headers get an i18n 'Required' marker when unmet, cleared once satisfied; tabHelpSpatialDefault gains 'A spatial relationship must be selected before Search is available.' New i18n keys searchBlockedNoValue/searchBlockedLoading/spatialBlockedNoDrawing/spatialBlockedNoResults/spatialBlockedNoRelationship/spatialBlockedNoLayers/requiredField. Cold-room tests for block-reason-utils authored by a separately launched agent from the brief (docs/specs/DCE_FEEDBACK_AUG2026_BRIEF.md). E2E validation.spec assertions expected to survive (Playwright >=1.57 honors aria-disabled). query-simple only, no shared-code, no FS bump. PRIOR r028.142 TODO #42 / DCE batch item 6: result-card action toolbar no longer bleeds onto the next card. Root cause (cold-reviewed brief docs/bugs/BUG-TOOLBAR-HEIGHT-001_BRIEF.md): the expanded card floor min-height 4.5rem (72px) was sized for a 2-icon stack (r023.34 comment says 64px), but the DEFAULT config renders 3 stacked icons (zoom+pan+trash, ~104px incl. 4px top offset + 2px gaps); the absolutely-positioned stack escaped the overflow:visible card (load-bearing for the dropdown, r024.41) and painted over the next card (~24px past the 8px inter-card margin). Fix (Adam ruled Option A, grow the card): data-action-count={1 + zoom? + pan?} on the card div (gates mirror the buttons: onZoomTo && !zoomOnResultClick / onPanTo && !panOnResultClick, config via singleton) + static emotion rule .result-item-expanded[data-action-count='3'] { min-height: 6.75rem } (108px = 4 + 3x32 + 2x2 + 4 breathing; border-box verified). 1-2 icon rows keep today's 72px exactly (the two inner feature-info 4.5rem floors are untouched BY DESIGN - they are what holds current density; see brief Section 3). Collapsed path (single button / dropdown) unaffected - the rule is scoped to result-item-expanded, the dropdown branch only renders when that class is absent. No automated coverage exists for this component (SimpleList mocked out in Jest); visual verification is Adam's manual pass in vertical AND horizontal modes (horizontal failure mode is clipping, not bleed). query-simple only, no shared-code, no FS bump. PRIOR r028.141 DCE feedback batch, trivial trio (docs/specs/DCE_FEEDBACK_AUG2026_BRIEF.md items 2/3/4). (a) "Zoom to selected" checkbox label-wrapped per the house pattern (interactive-draw-tool precedent) - it had NO accessible name and text-click did not toggle (DCE a11y); layout preserved (label carries mr-auto, margin-bottom 0). (b) Apply renamed Search: i18n apply key (both tabs share it), clearDrawing "when applying"->"when searching", tabHelpOperationsDefault "and Execute"->"and Search" (was stale either way); docs SPATIAL_RELATIONSHIPS_REFERENCE (2x Apply/Reset->Search/Reset) + CONFIGURATION_GUIDE:550; E2E validation.spec.ts selectors+comments /^Apply$/i->/^Search$/i (adapting existing spec to the renamed control, premise unchanged). Settings rebindApply NOT renamed (different button, the rebind tool). FLOW-02 already said "clicks Search" - now correct, left alone. helpers.clickApply in the OLD 1.19 install fixtures still says Apply - outside this repo, flagged to Adam, NOT touched. (c) ResultsModeControl sizes: labels 0.8125->0.875rem (the house body size, 16 uses runtime-wide), star glyph 0.6->0.7rem (parity with +/-), summary bar 0.8->0.8125rem (stays subordinate). DCE "very small text" on the New segment; Adam ruled bump both text and glyph. query-simple only, no shared-code, no FS bump. Items 1/5 (aria-disabled mechanism + spatial required indicators) and 6 (TODO #42 card growth) follow as separate versions. PRIOR r028.140 HelperSimple URL-param open now works for section-placed widgets, not just controllers (fixes the KC section-in-sidebar template where appActions.openWidget did nothing). New helper-simple runtime/widget-placement.ts: resolveWidgetSectionView walks layouts->views->sections to find the managed widget's section+view; clickSectionViewNavItem clicks the Navigator tab ExB renders with aria-controls={sectionId}_{viewId} (the REAL control, so ExB runs its own switch incl. opening a wrapping sidebar - a raw appActions.sectionNavInfoChanged dispatch had left the selector and the panel content out of sync). widget.tsx revealManagedWidget: section resolved => clickSectionViewNavItem (visible responsive instance, skips already-selected, polls ~1.8s for deep-link-on-load timing); null => original appActions.openWidget controller path (controllers fall through automatically, no special-casing). Chosen direction logged as helpersimple-reveal-strategy; nav-tab-not-found logs at BUG level (always console.warn) and falls back to controller open. Hash delivery (notifyManagedWidget) is unconditional in every path. +10 tests (widget-placement.test.ts: 6 resolver + 4 nav-click). helper-simple only; HS has no version.ts so this rides the QS family version, no shared-code so no FS bump. PRIOR r028.139 Test/debug hook: data-testid="tab-context-announcer" on the #38 live region (query-task.tsx). Collision-safe handle (QueryTask mounts per query item, so no semantic id; data-testid is duplicate-tolerant and the conventional E2E target). Invisible, a11y-neutral, no behavior change. query-simple only. PRIOR r028.138 Screen-reader announcements for programmatic context changes (TODO #38). One shared visually-hidden role=status/aria-live=polite/aria-atomic=true live region rendered once at the QueryTask root, fed by new useLiveAnnouncer hook (150ms debounce so rapid toggles collapse; same-message clear-then-reset-next-tick so identical text re-fires aria-live). Announces every context change the user's focus does NOT carry: programmatic tab switches (auto-to-Results WITH count, clear-back, query-switch, results back-button, restore) via a guard in the activeTab effect that skips when suppressTabAnnounceRef is set — handleTabChange (the user tablist path) sets that ref so its focus-read tab descriptions are not doubled. The Spatial Operations/Draw toggle announces via a new onAnnounce prop passed to SpatialTabContent (handleModeChange). Strings built by new PURE announce-utils.buildAnnouncement (reuses queryTab/spatialTab/resultsTab + spatialModeOperations/Draw labels + new i18n keys tabSwitchAnnouncement/tabSwitchAnnouncementResults/tabHelpModeAnnouncement, manual {token} replace). Focus-read tab descriptions, the r028.137 visual pulse, and reduced-motion handling are ALL unchanged (the live region complements them, does not replace). +tests: announce-utils (5) + useLiveAnnouncer fake-timer debounce/reset (4). No shared-code, no FS bump. query-simple only. PRIOR r028.137 Tab help polish: change-signal pulse on the "?" buttons (user request after Phase 4 team review - "not obvious the help is different when you change tabs"). One-shot halo pulse on the help button when its context changes: a theme-colored (var(--sys-color-primary-main)) ring fades from opacity 0.3 / scale 0.85 to 0 / 1.4 over 600ms ease-out. Implemented in the shared HelpPopoverButton so BOTH the main tab "?" and the Operations/Draw mode "?" get it: new contextId prop (activeTab / spatialMode); a ref-vs-prop comparison fires on every REAL change, PLUS once on mount (flashCount initialized to 1 - user call during smoke: the help is new, draw the eye on load; note every widget panel reopen pulses once too). Fires even if the popover was open, because the tab click light-dismisses it via autoClose and the pulse is then the only signal (user-reported during smoke). Keyed span re-mount restarts the animation cleanly. Timing tuned 600ms/0.30 -> 750ms/0.35 peak opacity per smoke feedback (a tad more present, not slower-feeling - ease-out keeps the bloom crisp), aria-hidden, pointer-events none, and animation: none under prefers-reduced-motion. No config (always-on subtle cue, not a feature). ALSO r028.137: SETTINGS debug logging for the tab-help config reads (user request - was missing): singletonConfigRead events from 'tab-help' (enabled, background, auto-contrast result, per-tab text source config-vs-default) and 'tab-help-mode' (mode text sources), matching the house pattern from the r028.050-054 singleton migration; visible via ?debug=SETTINGS. No shared-code change -> no FS bump. query-simple only. PRIOR r028.136 Tab help Phase 4: Operations/Draw mode help (TODO #36 extension, TAB_HELP_SPEC Phase 4, user request 2026-06-11). REPLACES the hardcoded r025.061 one-line mode description under the Spatial Operations/Draw toggle with the same "?" pattern as the main tabs: new SpatialModeHelp component (tab-help.tsx, which also got an internal HelpPopoverButton extraction shared by TabHelp and SpatialModeHelp - no behavior change to the main-tab button) rendered at the far right of the toggle row, popover describes the ACTIVE mode (heading = Operations/Draw toggle label), richer shipped defaults (tabHelpOperationsDefault/tabHelpDrawDefault) org-overridable via new config keys tabHelpOperationsText/tabHelpDrawText + shared-code getters getTabHelpOperationsText/getTabHelpDrawText (FS consumer bump r005.023). Master tabHelpEnabled switch and tabHelpBackgroundColor + auto-contrast filter down to the mode popover. SR: description spans for both modes; aria-describedby set DIRECTLY on the mode toggle buttons in SpatialTabContent (our own buttons, no innerRef indirection), gated on tabHelpEnabled. Removed now-dead i18n keys spatialModeOperationsDesc/spatialModeDrawDesc. Toggle row reflowed to flex (toggle flex:1 + "?" at far right, margins preserved). +tests: 2 getters. query-simple + shared-code. PRIOR r028.135 Tab help Phase 3: master switch + popup background (TODO #36, TAB_HELP_SPEC — completes the phased rollout). New config keys tabHelpEnabled (default true via !== false; false removes the "?" button AND the per-tab SR descriptions, with effect cleanup so no stale aria-describedby is left on the tab buttons) and tabHelpBackgroundColor (hex; blank/unset = theme surface) + shared-code getters getTabHelpEnabled/getTabHelpBackgroundColor (shared-code touched -> FS consumer bump r005.022). Text contrast is auto-computed, never configured: new runtime/contrast-utils.ts (parseHexColor #RGB/#RRGGBB + pickTextColorForBackground via YIQ >= 128 -> dark text #1a1a1a else white; unparseable falls back to dark text). Popover applies the background via --calcite-color-foreground-1 (the house error-popover pattern) + --calcite-color-text-1 and inline color on the content div. Settings: "Show tab help" Switch at the top of the Tab Help section, "Popup background" ThemeColorPicker, and a "Use theme default" reset button shown only when a color is set (clears to ''). +tests: getTabHelpEnabled/getTabHelpBackgroundColor in shared widget-config-manager.test.ts, contrast-utils in new QS tests/contrast-utils.test.ts. query-simple + shared-code. PRIOR r028.134 Tab help Phase 2: per-tab configurable markdown text (TODO #36, TAB_HELP_SPEC). Three new optional config keys tabHelpQueryText/tabHelpSpatialText/tabHelpResultsText (config.ts) + three shared-code singleton getters getTabHelpQueryText/getTabHelpSpatialText/getTabHelpResultsText (blank/whitespace = unset -> undefined so the runtime falls back to the shipped i18n default; shared-code touched -> feed-simple consumer bump r005.021). New "Tab Help" settings section: one TextArea per tab, value = configured text, placeholder = the shipped default (imported from runtime translations so there is one source of truth), live onChange config write (house TextInput pattern). TabHelp resolves text via the singleton per the Singleton Rule; popover body renders configured markdown through convertTemplateToHtml; the hidden SR description spans now run resolved text through new stripMarkdownToText (QS markdown-template-utils: convert through the real engine then strip tags/entities, so SR text always tracks what the popover renders). No app config seeding by design (unset = shipped default). +tests: 3 getters (configured/blank/whitespace/unset) in shared widget-config-manager.test.ts, stripMarkdownToText in new QS tests/markdown-strip.test.ts. Settings docs triple-update (SETTINGS_REFERENCE, SETTINGS_AT_A_GLANCE, CONFIGURATION_GUIDE). PRIOR r028.133 Tab help Phase 1 (TODO #36, docs/specs/TAB_HELP_SPEC.md). Single clickable "?" button at the right end of the tab strip (new runtime/components/tab-help.tsx) opening a calcite-popover that describes the ACTIVE tab: heading = tab name, body = shipped i18n default text (tabHelpQueryDefault/tabHelpSpatialDefault/tabHelpResultsDefault) rendered through the shared markdown engine (convertTemplateToHtml via the QS re-export; {{field}} tokens NOT substituted, no record context). Always on, theme styling, NO new config keys this phase (Phase 2 = per-tab configurable markdown text via shared-code getters -> FS bump; Phase 3 = master off switch + background color with auto-contrast text). SR layer: each tab button gets aria-describedby pointing at visually-hidden per-tab description spans; wired via the jimu Tab innerRef captured in query-task.tsx because TabProps forwards NO aria props (innerRef is typed in 1.20 but marked internal - re-verify on ExB upgrades). Popover follows the house click-driven pattern (controlled open + triggerDisabled + autoClose + closable + oncalcitePopoverClose, focus returned to the button on close via focusElementInKeyboardMode). Tab strip nav gets padding-right 38px so the absolutely-positioned button does not underlap the Results tab. query-simple only. PRIOR r028.132 Docs + public-distribution wiring (NO widget runtime change). New end-user docs/user-guide/SETTINGS_AT_A_GLANCE.md (every QS+HS setting: panel label, default, location) with 10 code-verified corrections from a 54-row agent sweep (Arrangement shows Vertical/Horizontal/Icon not Block/Inline/Popper; "Point zoom distance (ft)"; graphics ranges 1-10/8-32/1-6px; Dock Top/Bottom; Short ID is case-SENSITIVE; "Search by" DOES drive runtime UI; Display order is group-conditional; Export not gated by allowExport). Cross-linked SETTINGS_REFERENCE <-> at-a-glance and CONFIGURATION_GUIDE -> at-a-glance. Cleaned SPATIAL_RELATIONSHIPS_REFERENCE (2 TODO notes removed, broken FLOW-09 link fixed). Wired at-a-glance + DEBUG_LOGGER_GUIDE (+ drop-in debug-logger-standalone.ts) + SPATIAL_RELATIONSHIPS_REFERENCE into the public push (stage-public-release USER_DOCS). Added TODO #35 (settings level-set: retire forced-Simple paging dead code, allowExport dead key, Short ID case-sensitivity decision). docs/tooling only. PRIOR r028.131 New-widget default flips to Path 3 (FeatureLayer results in the LayerList) instead of Path 1 (highlight). Added "addResultsAsMapLayer": true to the widget DEFAULT config (query-simple/config.json), so a freshly-dropped QuerySimple widget starts in Path 3. Existing apps are intentionally NOT changed: the ~13 runtime reads stay === true, and config.json only seeds NEW widget instances, so any already-saved config (including an unset field, which stays Path 1) is honored. Needs smoke verification that ExB does not back-fill config.json defaults into existing saved configs (if it does, an existing unset widget would flip; fallback is to set it in the settings init instead). query-simple only. PRIOR r028.130 — Copy query to another widget Phase 3 (tests). Extracted the pure copy transformation into setting/query-copy-utils.ts (buildQueryCopyPayload) so it is unit-testable without React or the builder app-config API; handleCopyToWidget now delegates to it (identical behavior, just the Immutable-boundary conversions + editWidget apply). +11 tests in tests/query-copy-utils.test.ts (ID regen on target prefix, searchAlias/shortId _copy, same-index insert + clamp, no-mutation of caller arrays, output-DS clone with new id, useDataSources merge/no-dup). User confirmed same-index positioning is fine, so no position-picker was added. query-simple only. PRIOR r028.129 — Phase 2 (functional). handleCopyToWidget now also clones the source query's already-registered output data source for the new id (the DS JSON has no widget back-reference, so only id changes) and wires the source layer into the target widget's useDataSources, all applied with the builder editWidget(partialWidget, [outputDsJson]).exec() action (config + useDataSources + output DS in one). Replaces the Phase 1 editWidgetConfig. The copy is now runtime-functional in the target. Phase 3 = position control + tests. query-simple only. PRIOR r028.128 — Phase 1 (copy the query item). New "Copy to widget" command on each query item in settings (query-item-list.tsx): pick a target QuerySimple widget (discovered from the app config, uri 'widgets/query-simple/', excluding self), and the query is copied into that widget's config with a regenerated configId + outputDataSourceId (target prefix, reusing the Duplicate ID-regen) via the builder app-config action editWidgetConfig().exec(), inserted at the same index it had in the source. SidePopper target picker; i18n copyToWidget/copyPickTarget/copyNoTargets. PHASE 1 copies the query ITEM only — Phase 2 registers the new output DS for the target and ensures the target has the source layer; Phase 3 adds position control + tests. query-simple only. PRIOR r028.127 — LayerList removable-results fix. Ordering bug — result-feature-layer-factory.ts set __exb_layer_from_runtime=false BEFORE mapView.map.add(), but ExB's before-add listener force-sets it to true during the add, so the Remove button reappeared. Moved the assignment to AFTER map.add() (restores the r024.56 post-add design that the Path 2 -> Path 3 move lost). Not a framework change; gate is unchanged 1.19->1.20. query-simple only. PRIOR r028.126 — Rename grouped-query label "Search alias" to "Search by". — the runtime label above the group dropdown (query-task.tsx) and the settings section title/row/aria-labels (query-item-main-mode.tsx). Hardcoded UI strings (not i18n); text-only, no behavior change. query-simple only. PRIOR r028.125 — Hover feature highlight Phase 3: on/off toggles. Two new admin settings, hoverHighlightFeature and hoverPinEnabled (both default true via config?.x !== false), gate the highlight and the pin independently. New shared-code getters getHoverHighlightFeature / getHoverPinEnabled; query-result-item gates the highlight block on hoverHighlightEnabled and early-returns past the pin section when hoverPinEnabled is false. Settings: "Show pin on hover" Switch in the Hover Preview Pin section, "Show highlight on hover" Switch in the Hover Feature Highlight section. Defaults preserve prior behavior (both on). Shared-code touched (two getters) -> feed-simple consumer bump (r005.020). Completes the phased HOVER_HIGHLIGHT_SPEC (TODO #34). PRIOR r028.124 — Phase 2: configurable color. The highlight color is now an admin setting (hoverHighlightColor, default #EA4335 = pin red) via a new "Hover Feature Highlight" settings section + shared-code getter getHoverHighlightColor; query-result-item reads it, converts via hexToRgb, and re-applies the symbol on each show so a color change takes effect on the next hover. buildHoverHighlightSymbol now takes an rgb param. Shared-code touched (getter added) -> feed-simple consumer bump (r005.019). PRIOR r028.123 — Phase 1 (TODO #34, HOVER_HIGHLIGHT_SPEC). In addition to the hover preview pin, hovering a result card now also highlights the record's geometry on the map so it stands out from surrounding features (ArcIMS query-tool parity, community request). Mirrors the pin: a Graphic of data.getJSAPIGeometry() is drawn on mapView.graphics (red #EA4335 matching the pin — polyline=4px line, polygon=outline+faint fill, point=ring), tagged __hoverHighlight + __widgetId, created once per item and toggled visible on enter/leave, hidden on click, removed on unmount; simple-list hideHoverPins extended to clear it on pointerleave/scroll. Geometry-overlay approach (NOT layerView.highlight: the record maps to the Path 3 layer only via async COMPOSITE_KEY lookup, and direct-query stamps the ORIGIN layer on the graphic, so the overlay is simpler and works in Path 1 too). Phase 1 = no config (always on, hardcoded color); Phase 2 adds configurable color, Phase 3 adds on/off toggles for highlight + pin. query-simple only. PRIOR r028.122: Truncation alert now shows the ACTUAL total matching count, on BOTH query paths. When the service flags truncation we already fetch the true total via a count-only query (returnCountOnly); surface it in the alert. Form path (direct-query.ts): single where-clause query → trueMatchCount is EXACT; also extends the r028.121 spurious-suppression guard to the form path (if true total === returned, no real truncation → no alert). Spatial path (execute-spatial-query.ts): per-layer trueMatchCount = largest per-sub-query true count (exact for one input geometry, LOWER BOUND for several — unique union across geometries can't be cheaply deduped); query-task.tsx sums per-layer totals across truncated layers (distinct DS, no cross-layer dedup) and flags totalMatchCountIsLowerBound when >1 input geometry. Shared: TruncationAlertState gains totalMatchCount + totalMatchCountIsLowerBound; query-result.tsx picks the message — "matched {total}" (exact) / "matched at least {total}" (lower bound) / falls back to the generic "{limit}-record limit" wording when the count is unavailable (count fetch error). i18n: truncationAlertMessageCount, truncationAlertMessageCountAtLeast. Count-only runs only when flagged → zero overhead on the normal path. +7 tests (3 spatial trueMatchCount incl. multi-geometry max; 4 direct-query guard/count). query-simple only. Prior r028.121: spurious-flag guard (spatial). r028.120 instrumentation confirmed the Path 1 false positive: the "Parcels" MapServer set exceededTransferLimit=true on the MULTIPOINT sub-query (returned 11) even though the true matching count was also 11 (actuallyTruncated=false) — a known older-ArcGIS-Server quirk on multipart query geometries. Path 3's hosted layer doesn't have the quirk, hence no alert there for the same 69. Fix: in execute-spatial-query.ts the per-layer exceededTransferLimit now ORs subTruncated (= subExceeded && trueCount > returnedCount), NOT the raw service flag. When a sub-query is flagged we already fetch the true count via queryFeatureCount (returnCountOnly); if trueCount === returnedCount the flag is spurious and the alert is suppressed; if trueCount > returnedCount it's a genuine cap and the alert still fires. If the count query errors we fall back to trusting the flag (subTruncated=true) so a real truncation is never hidden. Count-only runs only on flagged sub-queries → zero overhead on the normal path. spatial-subquery-complete logging retained (SPATIAL tag) with countedAsTruncated showing what drove the decision. The genuine-cap path (trueCount > returnedCount) remains the domain of TODO #28 background pagination. query-simple only. Prior r028.119: spatial input assembly made event-driven. Investigating why the alert fires when no records were actually missed (Path 1 "Parcels" MapServer reports exceededTransferLimit=true at 69 records; Path 3 hosted "Parcels (HFL)" reports false at the same 69). execute-spatial-query.ts now logs a per-sub-query SPATIAL event (spatial-subquery-complete) with geometryIndex, geometryType, returnedCount, the sub-query's own exceededTransferLimit, and — ONLY when that sub-query is flagged — the TRUE matching count via queryFeatureCount (returnCountOnly, a few bytes, not subject to maxRecordCount), plus actuallyTruncated = trueCount > returnedCount. This distinguishes a genuine maxRecordCount cap (trueCount > returned) from a spurious MapServer flag (trueCount === returned). Does NOT change the alert behavior (still ORs featureSet.exceededTransferLimit per query-task.tsx:1256); the count-only fires only on flagged sub-queries so zero overhead on the normal path. Next step after reading the logs: decide between the count-only guard (suppress alert when not actuallyTruncated) and full background pagination (TODO #28). query-simple only. Prior r028.119 note: Spatial input assembly made event-driven (fixes r028.118 buffer-stays-on-screen regression). The r028.118 feature added a [...drawnGeometries] spread to the geometry-assembly useEffect; that effect re-ran on accumulatedRecords changes (post-query) and emitted a NEW array reference, which the buffer hook (keys inputGeometries by reference) read as "input changed" and redrew the buffer right after it was cleared. Root cause: watch-from-outside effect reacting to derived-state reference churn. Fix: replaced the assembly useEffect with event-driven recomputation — allInputGeometries is now rebuilt only when a real input event fires (draw end/edit/clear via updateDrawnGeometries, toggle onChange, mode change, smart-default tab-entry). Draw mode no longer depends on accumulatedRecords, so a post-query results change can't touch the draw-mode buffer. The ONE remaining effect syncs operations-mode input to the accumulatedRecords prop (its actual data source) + the draw-with-toggle-on cross case. Also removed the r028.118 reset useEffect (toggle reset moved into handleDrawCleared / post-query cleanup / Reset — the events that actually clear the draw). Refs (drawnGeometriesRef/spatialModeRef/includeResultsInputRef) let handlers read current state synchronously. Net: r028.118+119 add ZERO net-new useEffects vs the pre-feature baseline. query-simple only. Prior r028.118 note: Spatial Draw "Also include current results" opt-in (TODO #29). New Draw-mode checkbox in SpatialTabContent, shown only once a shape is drawn AND results exist (gated by hasDrawnGeometry && hasResults), default OFF, reset to off whenever it hides (mode switch / drawing cleared / results gone). When checked, the current result geometries (accumulatedRecords[].feature.geometry) are folded into the draw input in the geometry-assembly effect; the existing group-by-type unionOperator.executeMany then merges same-type drawn + result parts, so a drawn shape and already-selected results buffer/query together. No downstream changes: useBufferPreview and executeSpatialQuery already consume the multi-geometry array (executeSpatialQuery went multi-geometry in r028.101). No config field (runtime-only UI state). i18n key spatialIncludeResults. SR-mixing is a non-issue — the main query path normalizes result geometries to the map view SR (query-execution-handler.ts outSpatialReference), and drawn geometries are already in map SR. query-simple only. Prior r028.117 note: Field-table Phase 2.1 — per-field alias override. Admins can set a custom display label per field for SelectAttributes mode via a new sibling config map resultFieldAliases ({fieldName: alias}), shown as text inputs in the field picker (results-field.tsx). The alias is merged into fieldMeta once at the top of renderPopupContent, so all three render paths (on-map popup, result card, card-click popup) inherit it via the builders' existing meta.alias ?? name logic. Non-empty config alias wins over the layer schema alias; blank/whitespace falls back to schema alias, then field name. resultDisplayFields stays string[] (sibling map chosen over reshaping it to objects — 15 consumers + framework FieldSelector emit strings). rebind-utils remaps the alias-map keys alongside resultDisplayFields so labels survive a layer rebind. i18n: configFields/fieldAliases/fieldAliasPlaceholder. +4 tests. Gap note: r028.115 (configurable record limit) + r028.116 (its revert) were rolled back, never shipped — see docs/development/RECORD_LIMIT_FINDINGS.md. query-simple only. Phase 2.2 (format dropdown) next; see docs/specs/FIELD_TABLE_RENDERER_SPEC.md §9. Queries fetch up to the layer's max transfer count; previously a query returning exactly that count looked identical whether it was complete or truncated (exceededTransferLimit was captured by executeDirectQuery but DROPPED at the .then, and on the spatial path only logged). Now surfaced to the user via a calcite-popover that follows the existing no-results/query-error pattern (same query-feedback-anchor / spatial-feedback-anchor referenceElement, placement=top, scroll-into-view effect, amber warning styling). Plumbing: reducer TruncationAlertState + SET_TRUNCATION_ALERT + RESET_FOR_CLEAR wipe; main path forwards _directExceededTransferLimit and dispatches when true (cleared otherwise); spatial path dispatches if any target layer's exceededTransferLimit is set; both tabs (QueryTabContent, SpatialTabContent) render the popover. Message names the limit ("reached the {limit}-record limit"); we know the returned count = the cap but NOT the true total (would need a separate count query). i18n keys truncationAlert{Label,Title,Message}. The popover lives in the RESULTS PANEL (query-result.tsx, remove-feedback-anchor), NOT the input tabs: a successful query auto-switches to the Results tab, so a Spatial/Query-tab-anchored popover would mount on a now-hidden tab and never show (the original spatial-smoke miss). +5 reducer tests (new query-task-reducer.test.ts). query-simple only.

export const WIDGET_VERSION = MINOR_VERSION && Number(MINOR_VERSION) > 0
  ? `${BASE_VERSION}-r${RELEASE_NUMBER}.${MINOR_VERSION}`
  : `${BASE_VERSION}-r${RELEASE_NUMBER}`

