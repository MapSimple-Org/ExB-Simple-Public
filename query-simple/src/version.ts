/**
 * Widget version number.
 * Base version matches Experience Builder version (1.20.0).
 * Release number increments on major changes (001, 002, 003, ...).
 * Minor version increments on smaller fixes between major releases (r015.1, r015.2, ...).
 * 
 * To increment:
 * - Major change: Update RELEASE_NUMBER (e.g., 015 -> 016) and reset MINOR_VERSION to 0
 * - Minor fix: Increment MINOR_VERSION (e.g., 1 -> 2) while keeping same RELEASE_NUMBER
 */
const BASE_VERSION = '1.20.0'
const RELEASE_NUMBER = '028' // Increment on major changes (001, 002, 003, ...)
const MINOR_VERSION = '122' // r028.122 Truncation alert now shows the ACTUAL total matching count, on BOTH query paths. When the service flags truncation we already fetch the true total via a count-only query (returnCountOnly); surface it in the alert. Form path (direct-query.ts): single where-clause query → trueMatchCount is EXACT; also extends the r028.121 spurious-suppression guard to the form path (if true total === returned, no real truncation → no alert). Spatial path (execute-spatial-query.ts): per-layer trueMatchCount = largest per-sub-query true count (exact for one input geometry, LOWER BOUND for several — unique union across geometries can't be cheaply deduped); query-task.tsx sums per-layer totals across truncated layers (distinct DS, no cross-layer dedup) and flags totalMatchCountIsLowerBound when >1 input geometry. Shared: TruncationAlertState gains totalMatchCount + totalMatchCountIsLowerBound; query-result.tsx picks the message — "matched {total}" (exact) / "matched at least {total}" (lower bound) / falls back to the generic "{limit}-record limit" wording when the count is unavailable (count fetch error). i18n: truncationAlertMessageCount, truncationAlertMessageCountAtLeast. Count-only runs only when flagged → zero overhead on the normal path. +7 tests (3 spatial trueMatchCount incl. multi-geometry max; 4 direct-query guard/count). query-simple only. Prior r028.121: spurious-flag guard (spatial). r028.120 instrumentation confirmed the Path 1 false positive: the "Parcels" MapServer set exceededTransferLimit=true on the MULTIPOINT sub-query (returned 11) even though the true matching count was also 11 (actuallyTruncated=false) — a known older-ArcGIS-Server quirk on multipart query geometries. Path 3's hosted layer doesn't have the quirk, hence no alert there for the same 69. Fix: in execute-spatial-query.ts the per-layer exceededTransferLimit now ORs subTruncated (= subExceeded && trueCount > returnedCount), NOT the raw service flag. When a sub-query is flagged we already fetch the true count via queryFeatureCount (returnCountOnly); if trueCount === returnedCount the flag is spurious and the alert is suppressed; if trueCount > returnedCount it's a genuine cap and the alert still fires. If the count query errors we fall back to trusting the flag (subTruncated=true) so a real truncation is never hidden. Count-only runs only on flagged sub-queries → zero overhead on the normal path. spatial-subquery-complete logging retained (SPATIAL tag) with countedAsTruncated showing what drove the decision. The genuine-cap path (trueCount > returnedCount) remains the domain of TODO #28 background pagination. query-simple only. Prior r028.119: spatial input assembly made event-driven. Investigating why the alert fires when no records were actually missed (Path 1 "Parcels" MapServer reports exceededTransferLimit=true at 69 records; Path 3 hosted "Parcels (HFL)" reports false at the same 69). execute-spatial-query.ts now logs a per-sub-query SPATIAL event (spatial-subquery-complete) with geometryIndex, geometryType, returnedCount, the sub-query's own exceededTransferLimit, and — ONLY when that sub-query is flagged — the TRUE matching count via queryFeatureCount (returnCountOnly, a few bytes, not subject to maxRecordCount), plus actuallyTruncated = trueCount > returnedCount. This distinguishes a genuine maxRecordCount cap (trueCount > returned) from a spurious MapServer flag (trueCount === returned). Does NOT change the alert behavior (still ORs featureSet.exceededTransferLimit per query-task.tsx:1256); the count-only fires only on flagged sub-queries so zero overhead on the normal path. Next step after reading the logs: decide between the count-only guard (suppress alert when not actuallyTruncated) and full background pagination (TODO #28). query-simple only. Prior r028.119 note: Spatial input assembly made event-driven (fixes r028.118 buffer-stays-on-screen regression). The r028.118 feature added a [...drawnGeometries] spread to the geometry-assembly useEffect; that effect re-ran on accumulatedRecords changes (post-query) and emitted a NEW array reference, which the buffer hook (keys inputGeometries by reference) read as "input changed" and redrew the buffer right after it was cleared. Root cause: watch-from-outside effect reacting to derived-state reference churn. Fix: replaced the assembly useEffect with event-driven recomputation — allInputGeometries is now rebuilt only when a real input event fires (draw end/edit/clear via updateDrawnGeometries, toggle onChange, mode change, smart-default tab-entry). Draw mode no longer depends on accumulatedRecords, so a post-query results change can't touch the draw-mode buffer. The ONE remaining effect syncs operations-mode input to the accumulatedRecords prop (its actual data source) + the draw-with-toggle-on cross case. Also removed the r028.118 reset useEffect (toggle reset moved into handleDrawCleared / post-query cleanup / Reset — the events that actually clear the draw). Refs (drawnGeometriesRef/spatialModeRef/includeResultsInputRef) let handlers read current state synchronously. Net: r028.118+119 add ZERO net-new useEffects vs the pre-feature baseline. query-simple only. Prior r028.118 note: Spatial Draw "Also include current results" opt-in (TODO #29). New Draw-mode checkbox in SpatialTabContent, shown only once a shape is drawn AND results exist (gated by hasDrawnGeometry && hasResults), default OFF, reset to off whenever it hides (mode switch / drawing cleared / results gone). When checked, the current result geometries (accumulatedRecords[].feature.geometry) are folded into the draw input in the geometry-assembly effect; the existing group-by-type unionOperator.executeMany then merges same-type drawn + result parts, so a drawn shape and already-selected results buffer/query together. No downstream changes: useBufferPreview and executeSpatialQuery already consume the multi-geometry array (executeSpatialQuery went multi-geometry in r028.101). No config field (runtime-only UI state). i18n key spatialIncludeResults. SR-mixing is a non-issue — the main query path normalizes result geometries to the map view SR (query-execution-handler.ts outSpatialReference), and drawn geometries are already in map SR. query-simple only. Prior r028.117 note: Field-table Phase 2.1 — per-field alias override. Admins can set a custom display label per field for SelectAttributes mode via a new sibling config map resultFieldAliases ({fieldName: alias}), shown as text inputs in the field picker (results-field.tsx). The alias is merged into fieldMeta once at the top of renderPopupContent, so all three render paths (on-map popup, result card, card-click popup) inherit it via the builders' existing meta.alias ?? name logic. Non-empty config alias wins over the layer schema alias; blank/whitespace falls back to schema alias, then field name. resultDisplayFields stays string[] (sibling map chosen over reshaping it to objects — 15 consumers + framework FieldSelector emit strings). rebind-utils remaps the alias-map keys alongside resultDisplayFields so labels survive a layer rebind. i18n: configFields/fieldAliases/fieldAliasPlaceholder. +4 tests. Gap note: r028.115 (configurable record limit) + r028.116 (its revert) were rolled back, never shipped — see docs/development/RECORD_LIMIT_FINDINGS.md. query-simple only. Phase 2.2 (format dropdown) next; see docs/specs/FIELD_TABLE_RENDERER_SPEC.md §9. Queries fetch up to the layer's max transfer count; previously a query returning exactly that count looked identical whether it was complete or truncated (exceededTransferLimit was captured by executeDirectQuery but DROPPED at the .then, and on the spatial path only logged). Now surfaced to the user via a calcite-popover that follows the existing no-results/query-error pattern (same query-feedback-anchor / spatial-feedback-anchor referenceElement, placement=top, scroll-into-view effect, amber warning styling). Plumbing: reducer TruncationAlertState + SET_TRUNCATION_ALERT + RESET_FOR_CLEAR wipe; main path forwards _directExceededTransferLimit and dispatches when true (cleared otherwise); spatial path dispatches if any target layer's exceededTransferLimit is set; both tabs (QueryTabContent, SpatialTabContent) render the popover. Message names the limit ("reached the {limit}-record limit"); we know the returned count = the cap but NOT the true total (would need a separate count query). i18n keys truncationAlert{Label,Title,Message}. The popover lives in the RESULTS PANEL (query-result.tsx, remove-feedback-anchor), NOT the input tabs: a successful query auto-switches to the Results tab, so a Spatial/Query-tab-anchored popover would mount on a now-hidden tab and never show (the original spatial-smoke miss). +5 reducer tests (new query-task-reducer.test.ts). query-simple only.

export const WIDGET_VERSION = MINOR_VERSION && Number(MINOR_VERSION) > 0
  ? `${BASE_VERSION}-r${RELEASE_NUMBER}.${MINOR_VERSION}`
  : `${BASE_VERSION}-r${RELEASE_NUMBER}`

