/// <reference types="@arcgis/core/interfaces.d.ts" />
/// <reference path="../../index.d.ts" />
import { PublicLitElement as LitElement, TargetedEvent, JsxNode } from '@arcgis/lumina';
import { FieldDeleteOptions, IConfig } from '../../utils/types';

export declare class ArcgisPortalFieldDelete extends LitElement {
    /** For documentation links. */
    config?: IConfig;
    /** The fields to delete. */
    fieldNames?: string[];
    /** The layer whose field is being deleted. */
    layer?: __esri.FeatureLayer | __esri.OrientedImageryLayer;
    /** The MapView instance. */
    mapView?: __esri.MapView;
    /** Properties that may need to be passed in case values differ from what is available on Map Viewer. */
    options?: FieldDeleteOptions;
    /** Emitted before the modal is closed if the alert is handled in consumer. */
    readonly arcgisBeforeModalClose: TargetedEvent<this, string>;
    /** Emitted when the component is closed for any reason. */
    readonly arcgisClose: TargetedEvent<this, void>;
    /** Emitted when the field is successfully deleted. */
    readonly arcgisFieldsDeleted: TargetedEvent<this, void>;
}
