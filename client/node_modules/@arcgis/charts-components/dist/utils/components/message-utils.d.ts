import { WebChartTypes } from '../../spec';
import { ErrorStrings } from '../localization/interfaces';
/**
 * Pulls the proper placeholder according to the chart type
 *
 * @param chartType The chart type to get the placeholder for
 * @category Private
 */
export declare function getChartPlaceholder(chartType?: WebChartTypes, errorStrings?: ErrorStrings): string | undefined;
