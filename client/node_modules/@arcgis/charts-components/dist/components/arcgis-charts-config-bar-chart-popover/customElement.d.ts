/// <reference path="../../index.d.ts" />
import { IField, WebChartTemporalBinningUnits } from '../../spec';
import { JsxNode, PublicLitElement as LitElement, TargetedEvent } from '@arcgis/lumina';
import { PropertyValues } from 'lit';
import { IntervalPopoverProps, BarChartDataPopoverKinds } from '../../interfaces';

/** @private */
export declare class ArcgisChartsConfigBarChartPopover extends LitElement {
    /** Overwrite localized strings for this component */
    messageOverrides?: typeof this._messages._overrides;
    /**
     * Heading for the popover component
     *
     * @category Public
     */
    headingTitle?: string;
    /**
     * Reference element to show the popover.
     *
     * @category Public
     */
    referenceElement?: HTMLElement;
    /**
     * boolean to open or close the popover.
     *
     * @category Public
     * @default false
     */
    open?: boolean;
    /**
     * boolean to open or close the popover.
     *
     * @category Public
     */
    contentKind?: BarChartDataPopoverKinds;
    /**
     * Fields Information of the Feature layer
     *
     * @category Public
     */
    layerFieldsInfo: IField[];
    /**
     * Currently selected content (field names, for example)
     * to pass on to the popover.
     *
     * @category Public
     */
    selectedContent?: IntervalPopoverProps | string[] | string;
    /**
     * Flag to disable custom sort option.
     *
     * @category Public
     * @default false
     */
    isCustomSortDisabled: boolean;
    /**
     * Flag to disable aggregation type of `No Aggregation`.
     * `No Aggregation` is disabled for the bar chart created with only numeric fields.
     *
     * @category Public
     * @default false
     */
    isNoAggregationDisabled: boolean;
    /**
     * Calls `reposition()` method on popover element.
     *
     * @category Public
     */
    reposition(): Promise<void>;
    readonly arcgisChartsConfigBarChartDataPopoverChange: TargetedEvent<this, {
        contentKind?: BarChartDataPopoverKinds;
        value: string[] | [{
            intervalSize: number;
            intervalUnits: WebChartTemporalBinningUnits;
        }];
    }>;
    readonly arcgisChartsConfigPopoverClose: TargetedEvent<this, void>;
    private _messages;
}
