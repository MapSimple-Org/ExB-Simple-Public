import { WebChartStatisticType } from '../../../spec';
import { ChartConfig, CommonStrings } from '../../../utils';
import { SerialChartConfig } from '../../interfaces/chart-interfaces';
import { LayerInfo, SerialChartDataSortingKinds } from '../../interfaces/common';
/**
 * Generate default series value
 * @param {number | undefined } props.seriesIndex - The series index.
 * @param {string} props.xAxisField - The x-axis field.
 * @param {WebChartStatisticType} props.aggregationType - The aggregation type.
 * @param {SerialChartConfig} props.config - The chart config.
 * @param {LayerInfo | undefined} props.layerInfo - The layer info.
 * @param {CommonStrings} props.commonStrings - The common strings.
 */
export declare function getDefaultSeriesValue<T extends SerialChartConfig>(props: {
    seriesIndex?: number;
    xAxisField: string;
    aggregationType: WebChartStatisticType;
    config: T;
    layerInfo?: LayerInfo | undefined;
    commonStrings: CommonStrings;
}): (typeof props.config)["series"][0];
/**
 * Gets the proper chart title given the current config.
 * @param {SerialChartConfig} config - The chart config.
 * @param {LayerInfo} layerInfo - The layer info.
 * @param {CommonStrings} commonStrings - The common strings.
 */
export declare function getChartTitle(config: SerialChartConfig, layerInfo: LayerInfo, commonStrings: CommonStrings): string;
/**
 * Gets the aggregation type.
 * @param {ChartConfig} config
 * @returns {WebChartStatisticType | undefined}
 */
export declare const getAggregationType: (config: ChartConfig) => WebChartStatisticType;
/**
 * Gets the split-by field.
 * @param {ChartConfig} config
 * @returns {string}
 */
export declare const getSplitByField: (config: ChartConfig) => string;
/**
 * Gets the numeric fields.
 * @param {SerialChartConfig} config
 */
export declare const getNumericFields: (config: SerialChartConfig) => string[];
/**
 * Gets the sort order kind.
 */
export declare const getSortOrderKind: (config: ChartConfig) => SerialChartDataSortingKinds;
/**
 * Build a series array based on a list of numeric fields and aggregation type,
 * when the chart has no category field.
 * @param {string} props.xAxisField - The x-axis field.
 * @param {string[]} props.numericFields - The numeric fields to include in the series.
 * @param {WebChartStatisticType} props.aggregationType - The type of aggregation to use.
 * @param {SerialChartConfig} props.config - The chart configuration.
 * @param {LayerInfo} props.layerInfo - Information about the layer.
 * @param {CommonStrings} props.commonStrings - Common strings used in the chart.
 */
export declare function buildSeriesFromFields<T extends SerialChartConfig>(props: {
    xAxisField: string;
    numericFields: string[];
    aggregationType: WebChartStatisticType;
    config: T;
    layerInfo: LayerInfo;
    commonStrings: CommonStrings;
}): (typeof props.config)["series"];
/**
 * Build a series array based on a list of numeric fields and aggregation type.
 * @param {string} props.xAxisField - The x-axis field.
 * @param {string[]} props.numericFields - The numeric fields to include in the series.
 * @param {WebChartStatisticType} props.aggregationType - The type of aggregation to use.
 * @param {SerialChartConfig} props.config - The chart configuration.
 * @param {LayerInfo} props.layerInfo - Information about the data layer.
 * @param {CommonStrings} props.commonStrings - Common strings for localization.
 */
export declare function buildSeriesUsingNumericFields<T extends SerialChartConfig>(props: {
    xAxisField: string;
    numericFields: string[];
    aggregationType: WebChartStatisticType;
    config: T;
    layerInfo: LayerInfo;
    commonStrings: CommonStrings;
}): (typeof props.config)["series"];
/**
 * Build a series array based on a split-by field and aggregation type.
 * @param {string} props.splitByField - The split-by field.
 * @param {string} props.xAxisField - The x-axis field.
 * @param {string[]} props.numericFields - The numeric fields to include in the series.
 * @param {WebChartStatisticType} props.aggregationType - The type of aggregation to use.
 * @param {SerialChartConfig} props.config - The chart configuration.
 * @param {LayerInfo} props.layerInfo - Information about the layer.
 * @param {CommonStrings} props.commonStrings - Common strings used in the chart.
 */
export declare function buildSeriesUsingSplitByField<T extends SerialChartConfig>(props: {
    splitByField: string;
    xAxisField: string;
    numericFields: string[];
    aggregationType: WebChartStatisticType;
    config: T;
    layerInfo: LayerInfo;
    commonStrings: CommonStrings;
}): Promise<(typeof props.config)["series"]>;
/**
 * Builds a new series array based on the provided properties.
 *
 * **NOTE: The only async operation in this function is the call to `buildSeriesUsingSplitByField`. This means that it is recommended to call this function without `await` if a split-by field is not currently being used.**
 *
 * @param props.xAxisField - The field to use for the X-axis.
 * @param props.splitByField - The field to split the data by.
 * @param props.numericFields - The numeric fields to include in the series.
 * @param props.aggregationType - The type of aggregation to use.
 * @param props.config - The chart configuration.
 * @param props.layerInfo - Information about the data layer.
 * @param props.commonStrings - Common strings used in the chart.
 */
export declare const buildNewSeries: (props: {
    xAxisField: string;
    splitByField: string;
    numericFields: string[];
    aggregationType: WebChartStatisticType;
    config: SerialChartConfig;
    layerInfo: LayerInfo;
    commonStrings: CommonStrings;
}) => Promise<void>;
/**
 * Get the default series name.
 * @param {ChartConfig} props.config - The chart config.
 * @param {number} props.seriesIndex - The index of the series.
 * @param {LayerInfo} props.layerInfo - The layer info.
 * @param {ModelCommonStrings} props.commonStrings - The common strings.
 */
export declare const getDefaultSeriesName: (props: {
    config: SerialChartConfig;
    seriesIndex: number;
    layerInfo: LayerInfo;
    commonStrings: CommonStrings;
}) => string;
/**
 * Set the order by list in the config. The order by list is only used when the sort order kind is custom sort.
 * @param  {string[]} values - The order by list.
 * @param  {ChartConfig} config - The chart config.
 */
export declare const setOrderByList: (values: string[], config: ChartConfig) => void;
/**
 * Get the custom sort values from the config.
 * @param  {SerialChartConfig} config - The chart config.
 * @param  {LayerInfo} layerInfo - The layer info.
 *
 * @returns string[] - The custom sort values.
 */
export declare const getCustomSortValues: (config: SerialChartConfig, layerInfo: LayerInfo | undefined) => Promise<string[]>;
/**
 * Get whether temporal binning is enabled for the series in the config.
 * @param {SerialChartConfig} config - The chart config.
 * @returns {boolean} - Whether temporal binning is enabled.
 */
export declare function getBinTemporalData(config: SerialChartConfig): boolean;
/**
 * Enable or disable temporal binning for all series in the config.
 * @param {SerialChartConfig} config - The chart config.
 * @param {boolean} value - Whether to enable or disable temporal binning.
 *
 * **NOTE** This method removes any existing temporal binning settings if `value` is set to `false`.
 */
export declare function setBinTemporalData(value: boolean, config: SerialChartConfig): void;
