import { Color, ISimpleLineSymbol, WebChart, WebChartPieChartGroupSlice, WebChartPieChartSlice, WebChartStatisticType } from '../../spec';
import { ChartConfig, ChartTypes } from '../../utils';
import { ChartModel } from '../chart-model/chart-model';
import { ModelParams } from '../interfaces/chart-interfaces';
import { LayerInfo, ModelTypes, PieChartDataSortingKinds, PieChartSlicesDisplay, PieChartDataModes } from '../interfaces/common';
import { ModelWithColorMatch, ModelWithDataLabels, ModelWithTooltips } from '../mixins';
import { PieChartLegendModel } from './pie-chart-legend-model';
export declare class PieChartModel extends ChartModel {
    /**
     * Current configuration used for the pie chart
     */
    protected _config: ChartConfig<typeof ModelTypes.PieChart, WebChart>;
    protected _chartType: ChartTypes;
    /**
     * Protected property to hold the pie chart data mode (Category or Fields)
     *
     * `category` - The chart will use values from the category field to determine each slice.
     *
     * `fields` - The chart will use aggregated values from the numeric fields to determine each slice.
     *
     * @category Protected
     */
    protected _mode: PieChartDataModes;
    protected layerInfo: LayerInfo;
    /**
     * Array of hidden grouped slice ids
     * @category Private
     */
    private _groupedSliceIds;
    setup(params: ModelParams<ChartConfig<typeof ModelTypes.PieChart>>): Promise<void>;
    /**
     * Category field for the pie chart.
     */
    get category(): string;
    set category(category: string);
    /**
     * Numeric fields for the pie chart.
     */
    get numericFields(): string[];
    set numericFields(numericFields: string[] | string);
    /**
     * Aggregation type for the pie chart.
     */
    get aggregationType(): WebChartStatisticType;
    set aggregationType(aggregationType: WebChartStatisticType);
    /**
     * Gets the sort order for the pie chart.
     * @returns {PieChartDataSortingKinds}
     */
    getSortOrder(): PieChartDataSortingKinds;
    /**
     * Sets the sort order for the pie chart.
     * @param {PieChartDataSortingKinds} sortOrder The sort order.
     * @param {string[]} orderByList The list of category values.
     */
    setSortOrder(sortOrder: PieChartDataSortingKinds, orderByList?: string[]): void;
    /**
     * List of category's values for custom sort
     */
    get orderByList(): string[];
    set orderByList(orderByList: string[]);
    /**
     * The pie chart data mode (Category or Fields).
     * `category` - The chart is using values from the category field to determine each slice.
     * `fields` - The chart is using aggregated values from the numeric fields to determine each slice.
     */
    get mode(): PieChartDataModes;
    set mode(mode: PieChartDataModes);
    /**
     * Gets the list of the category values for the chart.
     * @returns {string[]}
     */
    getCustomSortValues(): Promise<string[]>;
    /**
     * Moves the custom sort values in-place for the order by list in the config.
     * **Note:** This method should be only used when the sort order is set to `customSort`.
     * @param {number} fromIndex The index to move the custom sort item from.
     * @param {number} toIndex The index to move the category item to.
     */
    moveCustomSortValuesInOrderByList(fromIndex: number, toIndex: number): void;
    /**
     * Ticks symbol connecting slices to labels.
     */
    get ticksSymbol(): ISimpleLineSymbol | undefined;
    set ticksSymbol(lineSymbol: ISimpleLineSymbol | undefined);
    /**
     * Array of slices for the pie chart.
     */
    get slices(): WebChartPieChartSlice[] | undefined;
    set slices(slices: WebChartPieChartSlice[] | undefined);
    /**
     * Array of grouped slice ids for the pie chart.
     */
    get groupedSliceIds(): string[];
    set groupedSliceIds(groupedSliceIds: string[]);
    /**
     * Slice grouping configuration for the pie chart.
     */
    get sliceGrouping(): WebChartPieChartGroupSlice | undefined;
    set sliceGrouping(sliceGrouping: WebChartPieChartGroupSlice | undefined);
    /**
     * Inner radius size for the pie chart.
     */
    get innerRadiusSize(): number;
    set innerRadiusSize(size: number);
    /**
     * Threshold below which slices will be grouped.
     */
    get groupingThreshold(): number;
    set groupingThreshold(threshold: number);
    /**
     * Character limit for the slice labels.
     */
    get labelCharacterLimit(): number | null;
    set labelCharacterLimit(limit: number | null);
    /**
     * Number of decimal places to display in the slice values.
     */
    get decimalPlaces(): number;
    set decimalPlaces(decimalPlaces: number);
    /**
     * Display type for the pie chart slices. Specifies whether to display the slices as a percentage, value, or both.
     */
    get displayType(): PieChartSlicesDisplay;
    set displayType(displayType: PieChartSlicesDisplay);
    protected generateDefaultConfig(): Omit<typeof this._config, "iLayer">;
    /**
     * Gets the original name of the slice based on the slice index.
     * @param {number} sliceIndex - The index of the slice.
     * @returns {Date | number | string | null | undefined}
     */
    getOriginalSliceName(sliceIndex: number): Date | number | string | null | undefined;
    /**
     * Gets the name of the slice based on the slice index. If the index is out of bounds, this function will return `undefined`.
     * @param {number} sliceIndex The index of the slice.
     * @returns {string | undefined}
     */
    getSliceName(sliceIndex: number): string | undefined;
    /**
     * Sets name of the slice based on the slice index.
     * @param {string} name The name for the slice.
     * @param {number} sliceIndex The index of the slice.
     */
    setSliceName(name: string, sliceIndex: number): void;
    /**
     * Gets the color of the slice based on the slice index. If the index is out of bounds, this function will return `undefined`.
     * @param {number} sliceIndex The index of the slice.
     * @returns {Color | undefined}
     */
    getSliceColor(sliceIndex: number): Color | undefined;
    /**
     * Sets the color of the slice based on the slice index.
     * @param {Color | undefined} color The color for the slice.
     * @param {number} sliceIndex The index of the slice.
     */
    setSliceColor(color: Color | undefined, sliceIndex: number): void;
    /**
     * Order of the slice array by returning an array of slice ids
     */
    get orderOfSlices(): string[] | undefined;
    set orderOfSlices(order: string[] | undefined);
    /**
     * Moves a series from one index in the series array to another.
     * @param {number} fromIndex Index to move the series from.
     * @param {number} toIndex Index to move the series to.
     */
    moveSeries(fromIndex: number, toIndex: number): void;
    /**
     * Gets the slice id for a given slice index. If the index is out of bounds, this function will return `undefined`.
     * @param {number} sliceIndex The index of the slice.
     * @returns {string | undefined}
     */
    getSliceId(sliceIndex: number): string | undefined;
    /**
     * Gets the slice index for a given slice id. If the `sliceId` is not found, this function will return `undefined`.
     * @param {string} sliceId The id of the slice.
     * @returns {number | undefined}
     */
    getSliceIndex(sliceId: string): number | undefined;
    /**
     * Gets the length of the slices array.
     * @returns {number}
     */
    getSlicesLength(): number;
    /**
     * Resets the slices styling to the default styling.
     */
    resetSlices(): void;
    /**
     * Returns whether the series styling is able to be reset.
     * @returns {boolean}
     */
    resetAvailable(): boolean;
    /**
     * Visibility of the ticks.
     */
    get ticksVisibility(): boolean;
    set ticksVisibility(visibility: boolean);
    /**
     * Start angle of the pie chart.
     */
    get startAngle(): number | null | undefined;
    set startAngle(angle: number | null | undefined);
    /**
     * End angle of the pie chart.
     */
    get endAngle(): number | null | undefined;
    set endAngle(angle: number | null | undefined);
    /**
     * Display category value on tooltip setting from the chart config.
     * @remarks If the setting is not found in the config, it will default to `true`.
     */
    get displayCategoryValueOnTooltip(): boolean;
    set displayCategoryValueOnTooltip(display: boolean);
    /**
     * Display numeric value on tooltip setting from the chart config.
     * @remarks If the setting is not found in the config, it will default to `true`.
     */
    get displayNumericValueOnTooltip(): boolean;
    set displayNumericValueOnTooltip(display: boolean);
    /**
     * Display percentage on tooltip setting from the chart config.
     * @remarks If the setting is not found in the config, it will default to `true`.
     */
    get displayPercentageOnTooltip(): boolean;
    set displayPercentageOnTooltip(display: boolean);
    /**
     * Prefix of the values of the slices.
     */
    get valuePrefix(): string | undefined;
    set valuePrefix(prefix: string | undefined);
    /**
     * Suffix of the values of the slice.
     */
    get valueSuffix(): string | undefined;
    set valueSuffix(suffix: string | undefined);
    /**
     * Prefix of the percentages of the slices.
     */
    get percentagePrefix(): string | undefined;
    set percentagePrefix(prefix: string | undefined);
    /**
     * Suffix of the percentages of the slices.
     */
    get percentageSuffix(): string | undefined;
    set percentageSuffix(suffix: string | undefined);
    /**
     * Offset for the data labels.
     */
    get dataLabelsOffset(): number;
    set dataLabelsOffset(offset: number);
    /**
     * Align data labels setting for the pie chart.
     */
    get alignDataLabels(): boolean | undefined;
    set alignDataLabels(align: boolean | undefined);
    /**
     * Setting for placing the data labels inside of the slices.
     */
    get dataLabelsInside(): boolean | undefined;
    set dataLabelsInside(inside: boolean | undefined);
    /**
     * Optimize data labels overlapping setting for the pie chart.
     */
    get optimizeDataLabelsOverlapping(): boolean | undefined;
    set optimizeDataLabelsOverlapping(optimize: boolean | undefined);
    /**
     * Generate the proper chart title given the current config.
     *
     * @returns {string}
     */
    generateChartTitle(): string;
}
export interface PieChartModel extends PieChartLegendModel, ModelWithTooltips, ModelWithColorMatch, ModelWithDataLabels {
}
