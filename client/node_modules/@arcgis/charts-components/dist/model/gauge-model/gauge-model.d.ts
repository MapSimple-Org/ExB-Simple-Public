import { GaugeChartSubTypes, ISimpleFillSymbol, WebChartStatisticType, ValueConversion, WebChartTextSymbol, WebGaugeChart } from '../../spec';
import { ChartTypes, ChartConfig } from '../../utils';
import { ChartModel } from '../chart-model/chart-model';
import { ModelParams } from '../interfaces/chart-interfaces';
import { LayerInfo, ModelTypes } from '../interfaces/common';
import { ModelWithBasicXAxis, ModelWithXGuides } from '../mixins';
export declare class GaugeModel extends ChartModel {
    /**
     * Generate the proper chart title given the current config.
     *
     * @returns {string}
     */
    generateChartTitle(): string;
    /**
     * Current configuration used for the histogram.
     */
    protected _config: ChartConfig<typeof ModelTypes.Gauge, WebGaugeChart>;
    protected layerInfo: LayerInfo;
    protected _chartType: ChartTypes;
    setup(params: ModelParams<ChartConfig<typeof ModelTypes.Gauge>>): Promise<void>;
    /**
     * Data field used for the gauge.
     */
    get field(): string;
    set field(field: string);
    /**
     * Aggregation type used for the gauge.
     *
     * Must be one of the values allowed by `WebChartStatisticType`, excluding `"no_aggregation"`.
     */
    get aggregationType(): WebChartStatisticType;
    set aggregationType(aggregationType: WebChartStatisticType);
    /**
     * Inner radius of the gauge.
     */
    get innerRadius(): number;
    set innerRadius(innerRadius: number);
    /**
     * Start angle of the gauge.
     */
    get startAngle(): number;
    set startAngle(startAngle: number);
    /**
     * End angle of the gauge.
     */
    get endAngle(): number;
    set endAngle(endAngle: number);
    /**
     * Conversion to apply to the gauge value.
     */
    get valueConversion(): ValueConversion | undefined;
    set valueConversion(valueConversion: ValueConversion | undefined);
    /**
     * Gets the index of the feature to be rendered.
     *
     * @remarks
     * This property will be ignored for a statistic gauge (indicated via `WebGaugeChart.subType` property).
     * For feature-based gauge, if this property is not present, the first feature will be used (featureIndex = 0)
     */
    get featureIndex(): number | undefined;
    set featureIndex(featureIndex: number | null | undefined);
    /**
     * Removes the existing feature index from the config object
     */
    removeFeatureIndex(): void;
    /**
     * Type of Gauge chart, like FeatureGauge and StatisticGauge.
     *
     * @remarks When this property is not specified, it will be defaulted to GaugeChartSubTypes.StatisticGauge.
     */
    get gaugeSubType(): GaugeChartSubTypes;
    /**
     * Visibility of the gauge's needle.
     */
    get needleVisibility(): boolean;
    set needleVisibility(visible: boolean);
    /**
     * Symbol of the gauge's needle.
     */
    get needleSymbol(): ISimpleFillSymbol | undefined;
    set needleSymbol(needleSymbol: ISimpleFillSymbol | undefined);
    /**
     * Start width of the gauge's needle.
     */
    get needleStartWidth(): number | null | undefined;
    set needleStartWidth(width: number | null | undefined);
    /**
     * End width of the gauge's needle.
     */
    get needleEndWidth(): number | null | undefined;
    set needleEndWidth(width: number | null | undefined);
    /**
     * Inner radius of the gauge's needle.
     */
    get needleInnerRadius(): number | null | undefined;
    set needleInnerRadius(innerRadius: number | null | undefined);
    /**
     * Whether to display the pin for the gauge's needle.
     */
    get needleDisplayPin(): boolean;
    set needleDisplayPin(displayPin: boolean);
    /**
     * Whether the inner label is visible.
     */
    get innerLabelVisible(): boolean;
    set innerLabelVisible(visible: boolean);
    /**
     * Content symbol for the inner label.
     */
    get innerLabelContent(): WebChartTextSymbol | undefined;
    set innerLabelContent(innerLabelContent: WebChartTextSymbol | undefined);
    /**
     * Whether axis ticks are visible.
     */
    get axisTickVisibility(): boolean;
    set axisTickVisibility(visibile: boolean);
    /**
     * Labels increment for the gauge.
     */
    get labelsIncrement(): number | undefined;
    set labelsIncrement(increment: number | undefined);
    /**
     * Whether to only show the first and last axis labels.
     */
    get onlyShowFirstAndLastLabels(): boolean;
    set onlyShowFirstAndLastLabels(showOnlyFirstAndLastLabels: boolean);
    protected generateDefaultConfig(): Omit<typeof this._config, "iLayer">;
}
export interface GaugeModel extends ModelWithBasicXAxis, ModelWithXGuides {
}
