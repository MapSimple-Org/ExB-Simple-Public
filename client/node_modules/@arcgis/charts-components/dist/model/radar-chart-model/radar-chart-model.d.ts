import { WebChart, WebChartRadarChartSeries } from '../../spec';
import { ChartConfig } from '../../utils';
import { SerialChartModel } from '../serial-chart-model/serial-chart-model';
import { ModelParams } from '../interfaces/chart-interfaces';
import { ModelWithBaselineStacking, ModelWithLineMarkerStyling } from '../mixins';
import { LayerInfo, ModelTypes } from '../interfaces/common';
export declare class RadarChartModel extends SerialChartModel {
    /**
     * Current configuration used for the Radar chart.
     */
    protected _config: ChartConfig<typeof ModelTypes.RadarChart, WebChart>;
    /**
     * Default series for the radar chart. Used in resetting the series styling.
     * @category Protected
     */
    protected defaultSeries: WebChartRadarChartSeries | undefined;
    protected _chartType: "radarSeries";
    protected layerInfo: LayerInfo;
    setup(params: ModelParams<ChartConfig<typeof ModelTypes.RadarChart>>): Promise<void>;
    protected generateDefaultConfig(): Omit<typeof this._config, "iLayer">;
    /**
     * Initializes the default series for the radar chart. Used in resetting the series styling.
     * @category Private
     */
    protected initDefaultSeries(): void;
    /**
     * Resets the series styling to the default styling.
     */
    resetSeriesStyling(): void;
    /**
     * Returns whether the series styling is able to be reset.
     * @returns {boolean}
     */
    resetAvailable(): boolean;
}
export interface RadarChartModel extends ModelWithBaselineStacking, ModelWithLineMarkerStyling {
}
