import { ChartModel } from '../chart-model/chart-model';
export declare abstract class ModelWithRotation extends ChartModel {
    /**
     * Whether the chart is rotated.
     */
    get rotatedState(): boolean;
    set rotatedState(rotated: boolean);
}
