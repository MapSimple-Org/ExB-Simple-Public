import { WebChartTextSymbol } from '../../spec';
import { ChartModel } from '../chart-model/chart-model';
export declare abstract class ModelWithDataLabels extends ChartModel {
    /**
     * Whether data labels are visible.
     * true: visible, false: hidden.
     */
    get dataLabelsVisibility(): boolean;
    set dataLabelsVisibility(newDataLabelsVisibility: boolean);
    /**
     * Data labels text symbol (font, color, style).
     */
    get dataLabelsSymbol(): WebChartTextSymbol | undefined;
    set dataLabelsSymbol(newDataLabelsSymbol: WebChartTextSymbol | undefined);
}
