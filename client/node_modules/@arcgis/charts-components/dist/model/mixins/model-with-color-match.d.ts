import { ChartModel } from '../chart-model/chart-model';
export declare abstract class ModelWithColorMatch extends ChartModel {
    /**
     * Whether the chart should attempt to match the data source's colors.
     *
     * `true`: Attempt to match the data source's colors.
     *
     * `false`: Use the colors defined in the config.
     */
    get colorMatch(): boolean;
    set colorMatch(newColorMatch: boolean);
}
