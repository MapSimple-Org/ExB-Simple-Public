import { Color, IDrawingInfo, LegendItemVisibility, NumberFormatOptions, SupportedLayer, WebChartDataFilters, WebChartTextSymbol } from '../../spec';
import { ModelParams } from '../interfaces/chart-interfaces';
import { LayerInfo, ModelTypes } from '../interfaces/common';
import { ChartConfig, ChartSubType, ChartTypes, CommonStrings } from '../../utils';
export declare abstract class ChartModel extends EventTarget {
    /**
     * Current configuration used for the chart.
     * @category Protected
     */
    protected _config: ChartConfig;
    /**
     * Grouping of layer-related properties to be passed to external function more compactly.
     * @category Protected
     */
    protected layerInfo: LayerInfo;
    /**
     * Common strings used in the model.
     * @category Protected
     */
    protected commonStrings: CommonStrings;
    /**
     * Chart Type
     * @category Protected
     */
    protected _chartType: ChartTypes;
    /**
     * List of setters (either props or methods) that have been changed and will require an async series rebuild.
     * @category Protected
     */
    protected dirtyAsyncSetters: string[];
    /**
     * Whether or not an async series update is currently in progress.
     * @category Protected
     */
    protected asyncConfigUpdateInProgress: boolean;
    /**
     * Function that resolves the setup promise.
     * @category Protected
     */
    protected resolveSetup?: () => void;
    /**
     * Completes the async setup process for the chart model. Must be called before using the model.
     * @param {ModelParams} params The parameters to be used to set up the chart model.
     * @async **This method is `async` and must be awaited for the changes to take effect.**
     */
    abstract setup(params: ModelParams<ChartConfig>): Promise<void>;
    /**
     * Checks if the current chart model is for the same chart as another chart model.
     * @param {ChartModel | undefined} otherModel The other chart model to compare to.
     */
    isSameChartAs(otherModel: ChartModel | undefined): boolean;
    /**
     * Generate the proper chart title from the current config.
     */
    abstract generateChartTitle(): string;
    /**
     * Data store object that can be used to store any data needed for the chart.
     */
    dataStore: any;
    /**
     * The current chart configuration object.
     */
    get config(): ChartConfig;
    set config(config: ChartConfig);
    /**
     * The chart type.
     */
    get chartType(): typeof this._chartType;
    /**
     * The associated layer.
     */
    get layer(): SupportedLayer | undefined;
    /**
     * The chart ID.
     */
    get chartId(): string | undefined;
    set chartId(id: string | undefined);
    /**
     * Promise that resolves when the setup process is complete.
     * **Note:** This promise does not need to be awaited if the `setup` method has already been awaited. It is simply provided as a way to await the setup process from other functions if needed to avoid race conditions.
     */
    setupFinished: Promise<void>;
    /**
     * The version of the chart configuration.
     */
    get version(): string | undefined;
    /**
     * The chart title text.
     * @type {string}
     */
    get titleText(): string;
    set titleText(title: string);
    /**
     * The chart subtitle text.
     * @returns {string}
     */
    get subtitleText(): string;
    set subtitleText(subtitle: string);
    /**
     * The chart description text.
     */
    get descriptionText(): string;
    set descriptionText(description: string);
    /**
     * Whether or not the chart title is visible.
     * `true`: Chart title is visible.
     * `false`: Chart title is hidden.
     * **Note:** Even if the title is visible, it may not be displayed if the title text is empty.
     */
    get chartTitleVisibility(): boolean;
    set chartTitleVisibility(visibility: boolean);
    /**
     * Whether or not the chart subtitle is visible.
     * `true`: Chart subtitle is visible.
     * `false`: Chart subtitle is hidden.
     * **Note:** Even if the subtitle is visible, it may not be displayed if the subtitle text is empty.
     */
    get chartSubtitleVisibility(): boolean;
    set chartSubtitleVisibility(visibility: boolean);
    /**
     * Whether or not the chart description is visible.
     * `true`: Description is visible.
     * `false`: Description is hidden.
     * **Note:** Even if the description is visible, it may not be displayed if the description text is empty.
     */
    get descriptionVisibility(): boolean;
    set descriptionVisibility(visibility: boolean);
    /**
     * The chart renderer.
     */
    get chartRenderer(): IDrawingInfo["renderer"] | undefined;
    set chartRenderer(renderer: IDrawingInfo["renderer"]);
    /**
     * The title text symbol. Contains styling information such as font, color, and symbol style.
     */
    get titleSymbol(): WebChartTextSymbol | undefined;
    set titleSymbol(titleSymbol: WebChartTextSymbol | undefined);
    /**
     * The subtitle text symbol. Contains styling information such as font, color, and symbol style.
     */
    get subtitleSymbol(): WebChartTextSymbol | undefined;
    set subtitleSymbol(subtitleSymbol: WebChartTextSymbol | undefined);
    /**
     * The description text symbol. Contains styling information such as font, color, and symbol style.
     */
    get descriptionSymbol(): WebChartTextSymbol | undefined;
    set descriptionSymbol(descriptionSymbol: WebChartTextSymbol | undefined);
    /**
     * The background color of the chart.
     */
    get backgroundColor(): Color | undefined;
    set backgroundColor(backgroundColor: Color | undefined);
    /**
     * Gets the visibility of a series.
     * @param seriesIndex The series index
     * @returns {boolean}
     */
    getSeriesVisibility(seriesIndex: number): boolean;
    /**
     * Sets the visibility of a series.
     * @param {LegendItemVisibility} props - Visibility options for the series.
     */
    setSeriesVisibility(props: LegendItemVisibility): void;
    /**
     * Gets the series ID for a given series index.
     * If the series index is out of bounds, this function will return `undefined`.
     *
     * @param {number} seriesIndex The index of the series.
     * @returns {string | undefined}
     */
    getSeriesId(seriesIndex: number): string | undefined;
    /**
     * Gets the series index for a given series ID.
     * If the series ID is not found, this function will return `undefined`.
     *
     * @param {string} seriesId The ID of the series.
     * @returns {number | undefined}
     */
    getSeriesIndex(seriesId: string): number | undefined;
    /**
     * The length of the series array.
     */
    get seriesLength(): number;
    /**
     * The chart sub type of the series.
     */
    get chartSubType(): ChartSubType;
    /**
     * Gets the data tooltip value format for a series.
     *
     * If the series index is out of bounds or the series list is empty, this function will return `undefined`.
     *
     * @param {number} seriesIndex The index of the series to retrieve the value format for.
     * @returns {NumberFormatOptions | undefined}
     */
    getDataTooltipValueFormat(seriesIndex: number): NumberFormatOptions | undefined;
    /**
     * Sets the data tooltip value format for a series.
     *
     * @param {number} seriesIndex The index of the series to set the value format for.
     * @param {NumberFormatOptions | undefined} valueFormat The value format to set.
     */
    setDataTooltipValueFormat(seriesIndex: number, valueFormat: NumberFormatOptions | undefined): void;
    /**
     * Whether to fetch `null` values for them to be counted or displayed as categories on the chart.
     */
    get fetchNULLValues(): boolean;
    set fetchNULLValues(fetchNULLValues: boolean);
    /**
     * The split-by values for the chart.
     */
    get splitByValues(): string[];
    /**
     * Gets the data filter for the bar chart.
     * @returns {WebChartDataFilters | undefined}
     */
    getDataFilters(): WebChartDataFilters | undefined;
    /**
     * Sets the data filter for the chart.
     * @param {WebChartDataFilters | undefined} newDataFilters The new data filter.
     */
    setDataFilters(newDataFilters: WebChartDataFilters | undefined): Promise<void>;
    /**
     * Emits a config update event.
     * @category Protected
     */
    protected emitConfigUpdate(setterName: string): void;
    /**
     * A function that generates the default config object when creating a new chart.
     * @category Protected
     */
    protected abstract generateDefaultConfig(): Omit<typeof this._config, "iLayer">;
    /**
     * A function that loads the config based on the provided props.
     * @category Protected
     * @param params.layer The layer to be used as the data source for the chart.
     * @param params.iLayer The layer description to use if the layer is not provided
     * @param params.config The config object to be loaded.
     */
    protected loadConfigFromLayer<T extends ModelTypes>(params: ModelParams<ChartConfig<T>>): Promise<void>;
    /**
     * Guards a setter from being called while an async config update is in progress so that the value being set is not overwritten by the async config update.
     * @param setter The setter callback to call after setup or async config update is finished.
     */
    protected guardSetterFromAsyncOverwrite(setter: (() => Promise<void>) | (() => void)): Promise<void>;
}
