import { WebChartDataFilters } from '../../../spec';
import { ChartConfig } from '../../../utils';
/**
 * Sets whether to fetch `null` values for them to be counted or displayed as categories on the chart.
 * @param fetchNULLValues Whether to fetch `null` values.
 * @param config The chart config.
 */
export declare const setFetchNULLValues: (fetchNULLValues: boolean, config: ChartConfig) => void;
/**
 * Gets whether to fetch `null` values for them to be counted or displayed as categories on the chart.
 * @param config The chart config.
 * @returns {boolean} Boolean value indicates whether NULL values will be fetched.
 */
export declare const getFetchNULLValues: (config: ChartConfig) => boolean;
/**
 * Gets the split by values from the chart config.
 * @param config The chart config.
 * @returns {string[]} An array of split by values.
 */
export declare const getSplitByValues: (config: ChartConfig) => string[];
/**
 * Get the data filter from the config.
 * @param  {ChartConfig} config - The chart config.
 * @returns WebChartDataFilters | undefined - The data filter.
 */
export declare const getDataFilters: (config: ChartConfig) => WebChartDataFilters | undefined;
/**
 * Set the data filter in the config.
 * @param  {WebChartDataFilters | undefined} dataFilter - The data filter.
 * @param  {ChartConfig} config - The chart config.
 */
export declare const setDataFilters: (dataFilter: WebChartDataFilters | undefined, config: ChartConfig) => void;
