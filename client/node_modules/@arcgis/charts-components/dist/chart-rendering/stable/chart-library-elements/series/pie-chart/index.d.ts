import { PieChart, PieSeries } from '@amcharts/amcharts4/charts.js';
import { PieSlicesSymbols, QueryObject, SelectionTheme, UtilsStrings } from '../../../../../utils';
import { WebChart, WebChartDataItem, WebChartPieChartSeries } from '../../../../../spec';
import { getSliceSymbol } from '../../../../common/series/pie-chart';
import { LoaderHandle } from '../../loading-indicator/interfaces';
import { UpdatePieChartSeriesProps } from './interfaces';
/**
 * This function sets the series default colors, overriding the default amCharts theme.
 * This is made to prevent color glitches making the default theme appear momentarily when updating the
 * chart's data (refer to https://devtopia.esri.com/WebGIS/arcgis-charts/issues/6005).
 * This function needs to be called **before** the slices are populated (after a data change) otherwise it doesn't have any effect.
 * It also means that workaround is only effective when the data changes, which seems to fit the conditions triggering
 * the bug anyway.
 *
 * @param props.chartData The pie chart's data
 * @param props.pieSeries The pie chart series instance
 * @param props.seriesConfig The pie chart series config
 * @param props.pieSlicesSymbols The pre-computed slices symbol
 */
declare function overrideSeriesDefaultColors(props: {
    chartData?: WebChartDataItem[];
    pieSeries: PieSeries;
    seriesConfig: WebChartPieChartSeries;
    pieSlicesSymbols?: PieSlicesSymbols;
}): void;
export declare function initializePieChartSeries(props: {
    seriesConfig: WebChartPieChartSeries;
    hideLoaderHandle: LoaderHandle["hideLoader"];
    selectionTheme?: SelectionTheme;
}): PieSeries;
/**
 * Generates a slices array containing the slices id, label and style.
 *
 * @param props.chart The pie chart
 * @param props.chartConfig The chart config
 * @param props.queryObject The query object (layer or layer view)
 * @param props.usePopupTemplateFieldsInfo Whether to use the popup template info for the aliases
 * @param props.labelUpdateNeeded Whether the labels were updated
 */
export declare function computePieChartSlices(props: {
    chart: PieChart;
    chartConfig: WebChart;
    queryObject: QueryObject;
    usePopupTemplateFieldsInfo?: boolean;
    labelUpdateNeeded: boolean;
    utilsStrings: UtilsStrings;
}): Promise<PieSlicesSymbols>;
/**
 * Function updating chart series and assign its data
 *
 * @param props
 */
export declare function updatePieChartSeries(props: UpdatePieChartSeriesProps): void;
export declare const seriesPiechartPrivateFunctions: {
    overrideSeriesDefaultColors: typeof overrideSeriesDefaultColors;
    getSliceSymbol: typeof getSliceSymbol;
};
export {};
