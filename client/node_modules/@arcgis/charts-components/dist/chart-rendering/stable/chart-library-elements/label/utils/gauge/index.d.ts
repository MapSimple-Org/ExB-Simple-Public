import { Axis, AxisDataItem, AxisRendererCircular, GaugeChart } from '@amcharts/amcharts4/charts.js';
import { Label } from '@amcharts/amcharts4/core.js';
import { WebChartGaugeAxis } from '../../../../../../spec';
import { AutoResizeCallbackType } from './interfaces';
/**
 * Disposes a specific auto-resize callback. It disposes it and updates the chart's memory storage.
 *
 * @param chart The gauge chart
 * @param type The type of callback to dispose
 */
declare function disposeResizeCallback(chart: GaugeChart, type: AutoResizeCallbackType): void;
/**
 * Adjusts a scale so a given font size can stay within the min/max limits and bounds defined for a label.
 *
 * @param props.scaleToAdjust The current scale value
 * @param props.fontSize The base font size
 * @param props.type The type of label to handle (axis label, inner label or first/last labels)
 * @param props.chart The gauge chart
 * @param props.labelList The label list
 * @param props.axisConfig The axis config
 * @returns The adjusted scale
 */
declare function adjustLabelScale(props: {
    scaleToAdjust: number;
    fontSize: number;
    type: AutoResizeCallbackType;
    chart: GaugeChart;
    labelList: Label[];
    axisConfig?: WebChartGaugeAxis;
}): number;
/**
 * Check the list of data items to make sure at least there is one has a value
 *
 * @param list The list of axis data items
 * @returns
 */
export declare function listHasAtLeastOneValidValue(list: AxisDataItem[]): boolean;
/**
 * Handles the auto-resize feature on the axis labels. It is enabled when no font size is provided by the config.
 *
 * @param props.chart The chart
 * @param props.axisConfig The config for the axis
 * @param props.axis The axis
 */
export declare function handleAxisLabelsAutoResizing(props: {
    chart: GaugeChart;
    axisConfig: WebChartGaugeAxis;
    axis: Axis<AxisRendererCircular>;
}): void;
/**
 * Handles the auto-resize feature on the inner label. It is enabled when no font size is provided by the config.
 *
 * @param props.chart The chart
 * @param props.innerLabelConfig The config for the inner label
 * @param props.innerLabel The inner label
 */
export declare function handleInnerLabelAutoResizing(chart: GaugeChart, innerLabel: Label): void;
/**
 * Handles the auto-resize feature on the first&last labels. It is enabled when no font size is provided by the config and
 * the `onlyShowFirstAndLastLabels` option is `true`.
 *
 * @param props.chart The chart
 * @param props.axisConfig The config for the axis
 * @param props.axis The axis
 */
export declare function handleFirstAndLastLabelsAutoResizing(props: {
    chart: GaugeChart;
    axisConfig: WebChartGaugeAxis;
    axis: Axis<AxisRendererCircular>;
}): void;
export declare const utilsGaugePrivateFunctions: {
    adjustLabelScale: typeof adjustLabelScale;
    disposeResizeCallback: typeof disposeResizeCallback;
};
export {};
