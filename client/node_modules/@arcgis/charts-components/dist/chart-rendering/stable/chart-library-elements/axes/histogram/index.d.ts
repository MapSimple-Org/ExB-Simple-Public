import { AxisDataItem } from '@amcharts/amcharts4/charts.js';
import { InitializeHistogramAxesProps, UpdateHistogramAxesProps } from './interfaces';
/**
 * Finds the width from the longest label
 *
 * @param axisTicks Array of ticks with labels
 */
declare function getLongestLabelWidth(axisTicks: AxisDataItem[]): number;
export declare function initializeHistogramAxes(props: InitializeHistogramAxesProps): void;
/**
 * Function to update the horizontal and vertical axes of the histogram
 * During the update stage some axes may have gotten re-initialized,
 * so in those cases, we will need to add event listeners again.
 * Listener listens to change in axis size and show/hide labels
 * accordingly.
 *
 * @param props
 */
export declare function updateHistogramAxes(props: UpdateHistogramAxesProps): Promise<void>;
export declare const axesHistogramPrivateFunctions: {
    getLongestLabelWidth: typeof getLongestLabelWidth;
};
export {};
