import { Axis, AxisDataItem, AxisRenderer, AxisRendererCircular } from '@amcharts/amcharts4/charts.js';
import { WebChartGuide, WebChartAxisTypes } from '../../../../spec';
import { UtilsStrings } from '../../../../utils';
import { AxisDataItemType } from '../../../common/guides-and-ticks';
import { GuideTooltipFormatCallback } from '../../../common/interfaces';
import { UpdateTicksProps } from './interfaces';
/**
 * Return the guide input value. If the input string represents a percentage value, calculate and return the equivalent decimal value.
 *
 * @param props.inputValue the input value
 * @param props.axisType the type of axis.
 * @param props.axisMin the axis min value; required when guide's start and end values are input as percentage
 * @param props.axisMax the axis max value; required when guide's start and end values are input as percentage
 */
declare function convertGuideValue(props: {
    inputValue: Date | number | string | null | undefined;
    axisType: WebChartAxisTypes;
    axisMin?: number;
    axisMax?: number;
}): Date | number | string | null | undefined;
/**
 * Retrieve the start and end values of the guide from the config. The process also tries to convert
 * the value accordingly (depending on whether it's a Date, Percentage, decimal, etc).
 *
 * @param axis The axis to which the guide attaches.
 * @param guideConfig The guide's config
 */
export declare function getGuideValuesFromConfig<T extends Axis>(axis: T, guideConfig?: WebChartGuide): {
    isValidGuide: boolean;
    start?: Date | number | string | null;
    end?: Date | number | string | null;
};
/**
 * Returns an array of axis ticks, axis guides or special guides for the gauge.
 *
 * @param axis The axis we want the ticks from
 * @param dataItemType The type of element to return
 */
export declare function getTickOrGuideList(axis: Axis<AxisRenderer | AxisRendererCircular>, dataItemType: AxisDataItemType): AxisDataItem[];
export declare function updateGuidesArray<T extends Axis>(props: {
    axis: T;
    guides?: WebChartGuide[];
    guideTooltipFormatter?: GuideTooltipFormatCallback;
    utilsStrings: UtilsStrings;
}): void;
export declare function updateTicksArray(props: UpdateTicksProps): void;
export declare const guidesAndTicksPrivateFunctions: {
    convertGuideValue: typeof convertGuideValue;
};
export {};
