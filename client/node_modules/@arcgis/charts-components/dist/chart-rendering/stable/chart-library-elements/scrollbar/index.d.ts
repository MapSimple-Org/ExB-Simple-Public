import { Scrollbar } from '@amcharts/amcharts4/.internal/core/elements/Scrollbar.js';
import { Chart } from '@amcharts/amcharts4/charts.js';
import { WebChartAxisScrollBar } from '../../../../spec';
/**
 * Initializes the scrollbar for the chart
 *
 * @param chart The chart
 */
export declare function initializeChartScrollbar(chart: Chart): void;
/**
 * Updates a scrollbar's focusable property.
 * We disable it when the scrollbar is hidden to avoid the focus to bet set on an invisible object.
 * See https://devtopia.esri.com/WebGIS/arcgis-charts/issues/7589
 *
 * @param scrollbar The scrollbar to update
 * @param visible The scrollbar visibility
 */
declare function updateScrollbarFocus(scrollbar: Scrollbar, visible: boolean): void;
/**
 * Updates scrollbar on the chart based on input value from scrollbar
 *
 * @param props.chart The chart
 * @param props.scrollbarXConfig The horizontal scrollbar config
 * @param props.scrollbarYConfig The vertical scrollbar config
 */
export declare function updateChartScrollbar(props: {
    chart: Chart;
    scrollbarXConfig?: WebChartAxisScrollBar;
    scrollbarYConfig?: WebChartAxisScrollBar;
}): void;
export declare const chartScrollbarPrivateFunctions: {
    updateScrollbarFocus: typeof updateScrollbarFocus;
};
export {};
