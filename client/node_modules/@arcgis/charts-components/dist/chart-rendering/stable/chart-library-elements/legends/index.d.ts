import { SerialChart, Legend, Series } from '@amcharts/amcharts4/charts.js';
import { Label, ListTemplate } from '@amcharts/amcharts4/core.js';
import { WebChartSeriesType, WebChartTypes } from '../../../../spec';
import { LegendElements } from '../../interfaces';
import { UpdateLegendProps } from './interfaces';
/**
 * Checks if the legend label and value label are misplaced, and swap them if so.
 * The reason why it happens is not clear (see issue https://devtopia.esri.com/WebGIS/arcgis-charts/issues/4750)
 *
 * @param legend The legend
 */
declare function handleMisplacedLabels(legend: Legend): void;
/**
 * Enforces the legend labels opacity according to their corresponding series visibility;
 * If series visibility is not provided, it will be defaulted to true.
 *
 * If `sliceCategory` is defined, it will ignore check to seriesId
 *
 * @param props.labels List of labels that are used to match with it's corresponding series to set the opacity
 * @param props.seriesId Passing the seriesId first if label opacity is to be applied to (Bar chart, Histogram, Scatterplot, Line chart or Combo chart)
 * @param props.sliceCategory Slice category value of any slice of the Pie Chart
 */
export declare function reapplyLabelOpacity(props: {
    labels: ListTemplate<Label>;
    seriesId?: string;
    sliceCategory?: string;
}): void;
/**
 * Assigns the appropriate label opacity to a chart's legend when there is a chart update
 *
 * @param series The chart series of which legend labels are to be set.
 */
export declare function handleLegendLabelsOpacity(series: Series): void;
/**
 * Sets up legend items order for Histogram Or PieChart
 *
 * @param legend
 */
declare function orderHistogramOrPieChartLegendItems(legend: Legend): boolean;
/**
 * Builds the legend order key based on chart or chart config
 * Each keyCombination has a total of 6 parts in the order of rotatedKey, rtlKey, stackedKeyType, legendPositionKey, axisTypeKey and orderByFieldsKey
 *
 * @param legend legend for building the key
 */
declare function buildLegendOrderKey(legend: Legend): string;
/**
 * Sets up Legend items order for Bar and Line charts
 *
 * @param legend the legend for bar/line chart
 */
declare function orderBarLineChartLegendItems(legend: Legend): boolean;
/**
 * Sets up the legend items in the proper order for the chart's stacking and rotation
 *
 * @param legendElements The legend elements
 * @param chartType The chart type
 */
declare function orderLegendItems(legendElements: LegendElements, chartType?: WebChartTypes): void;
/**
 * Allows to modify the shape of the legend markers. By default they are rounded rectangles,
 * and according to the `webChartLegend.roundMarkers` value they can be rendered as circles.
 *
 * @param legend The legend object containing the markers
 */
declare function updateLegendMarkerShape(legend: Legend): void;
/**
 * Initializes the legend with default values and assign it to the chart.
 * Set adapters on some properties.
 *
 * @param chart The chart on which to attach the legend
 */
export declare function initializeLegend(chart: SerialChart, seriesConfig: WebChartSeriesType[]): LegendElements;
/**
 * Updates the legend
 *
 * @param props
 */
export declare function updateLegend(props: UpdateLegendProps): void;
export declare const legendPrivateFunctions: {
    handleMisplacedLabels: typeof handleMisplacedLabels;
    updateLegendMarkerShape: typeof updateLegendMarkerShape;
    orderHistogramOrPieChartLegendItems: typeof orderHistogramOrPieChartLegendItems;
    orderBarLineChartLegendItems: typeof orderBarLineChartLegendItems;
    orderLegendItems: typeof orderLegendItems;
    buildLegendOrderKey: typeof buildLegendOrderKey;
};
export {};
