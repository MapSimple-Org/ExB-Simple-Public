import { Chart, GaugeChart, PieChart, XYChart } from '@amcharts/amcharts4/charts.js';
import { WebChart, WebGaugeChart, WebChartTypes } from '../../spec';
import { CursorRange, ErrorStrings, QueryObject, UtilsStrings, ActionModes } from '../../utils';
import { AnyConfigType, ChartInstance, ChartOptions, CreateChartProps, GlobalOptions, XYConfigWithILayer } from '../common/interfaces';
import { ChartResult } from './interfaces';
/**
 * Applies the action mode (selection, zoom...) to the chart. It will change the cursor behavior as well as the
 * selection events.
 *
 * @param props.chart The chart
 * @param props.actionMode The action mode (selection, zoom...)
 * @param props.cursorRange The cursor range (multi-axes, x-axis...)
 */
declare function setActionMode(props: {
    chart: GaugeChart | PieChart | XYChart;
    actionMode?: ActionModes;
    cursorRange?: CursorRange;
}): void;
declare function activateMobileFeatures(chart: GaugeChart | PieChart | XYChart, enableResponsiveFeatures?: boolean): void;
/**
 * Initializes the chart axes
 *
 * @param props.chart The chart
 * @param props.axesConfig The axes configuration
 * @param props.chartType The chart type
 * @param props.isRotated Indicates if the chart is rotated
 * @param props.errorStrings The t9n error strings
 */
declare function initializeAxes(props: {
    chart: Chart;
    axesConfig: WebChart["axes"];
    chartType?: WebChartTypes;
    isRotated?: boolean;
    errorStrings: ErrorStrings;
}): Promise<void>;
/**
 * Creates a chart
 *
 * @param props.chartContainer The container where the chart will be rendered
 * @param props.chartConfig The chart configuration
 * @param props.queryObject The query object (layer or feature layer view)
 * @param props.options The chart runtime options
 * @param props.globalOptions The global options
 * @param props.t9nStrings The t9n strings
 * @returns
 */
export declare function createXYChart(props: CreateChartProps<XYConfigWithILayer>): Promise<ChartResult>;
/**
 * Creates and returns an instance of PieChart
 *
 * @param props
 */
export declare function createPieChart(props: {
    chartContainer?: HTMLElement;
    chartConfig?: WebChart;
    queryObject?: QueryObject;
    utilsStrings: UtilsStrings;
    errorStrings: ErrorStrings;
    options?: ChartOptions;
    globalOptions?: GlobalOptions;
}): Promise<ChartResult>;
/**
 * Creates and returns n instance of GaugeChart
 *
 * @param props
 */
export declare function createGaugeChart(props: {
    chartContainer?: HTMLElement;
    chartConfig?: WebGaugeChart;
    queryObject?: QueryObject;
    utilsStrings: UtilsStrings;
    errorStrings: ErrorStrings;
    options?: ChartOptions;
    globalOptions?: GlobalOptions;
}): Promise<ChartResult>;
/**
 * Creates a chart and returns an object of type `ChartInstance` containing a set of functions
 * in relation with the created chart.
 *
 * @param props.chartContainer The HTML element where the chart will be rendered
 * @param props.chartConfig The chart configuration
 * @param props.options The chart runtime options
 * @param props.globalOptions The global options
 * @param props.t9nStrings The t9n strings
 */
export declare function createChart(createChartProps: CreateChartProps<AnyConfigType>): Promise<ChartInstance>;
export declare const stablePrivateFunctions: {
    activateMobileFeatures: typeof activateMobileFeatures;
    setActionMode: typeof setActionMode;
    initializeAxes: typeof initializeAxes;
};
export {};
