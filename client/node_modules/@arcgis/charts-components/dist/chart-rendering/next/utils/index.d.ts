import { Chart, Sprite } from '@amcharts/amcharts5';
/**
 * Returns a unique ID associated to a chart
 *
 * @param chart
 */
export declare function getChartUniqueInternalID(chart: Chart): string;
/**
 * Indicates whether an amCharts element is still available (non null, non undefined, non disposed)
 *
 * @param element
 */
export declare function isElementValid(element: Sprite): boolean;
/**
 * Converts a color represented by a string into a RGBa object
 *
 * @param input The color to convert
 */
export declare function hexColorToRGB(input: string): {
    r: number;
    g: number;
    b: number;
    a?: number;
} | undefined;
/**
 * Determines if an RGBA color is light or dark, considering opacity.
 * @param {number} props.r - Red (0-255)
 * @param {number} props.g - Green (0-255)
 * @param {number} props.b - Blue (0-255)
 * @param {number} props.a - Alpha (0-1)
 * @returns boolean
 */
export declare function isColorLight(props: {
    r: number;
    g: number;
    b: number;
    a?: number;
}): boolean;
