import { StorageType } from './interfaces';
type AllStorageTypes = StorageType["axis"] | StorageType["chart"] | StorageType["guide"] | StorageType["label"] | StorageType["legend"] | StorageType["marker"] | StorageType["series"];
type AllStorageKeys = keyof StorageType["axis"] & keyof StorageType["chart"] & keyof StorageType["guide"] & keyof StorageType["label"] & keyof StorageType["legend"] & keyof StorageType["marker"] & keyof StorageType["series"];
/**
 * Disposes a specific storage. To be used when a chart is disposed for instance
 *
 * @param memoryStorageId
 */
export declare function deleteStorage(storageKey?: number): void;
/**
 * Retrieves a legend property from a specific storage.
 * @param storageKey The legend id
 * @param propertyName The property to retrieve
 */
export declare function getValueFromLegendStorage<T>(storageKey: number, propertyName: AllStorageKeys): T | undefined;
/**
 * Updates a legend property from a specific storage
 * @param storageKey The legend id
 * @param informationToStore An object containing the properties to update. It will append and replace the existing ones for the given storage key and id.
 */
export declare function updateLegendStorage(storageKey: number, informationToStore: AllStorageTypes): void;
/**
 * Updates a specific storage
 *
 * @param storageKey The key to update (chart id, label id...)
 * @param informationToStore An object containing the properties to update. It will append and replace the existing ones for the given storage key and id.
 */
declare function updateStorage(storageKey: number, informationToStore: AllStorageTypes): void;
/**
 * Retrieves a property from a specific storage
 *
 * @param storageKey The key to update (chart id, label id...)
 * @param propertyName The property to retrieve
 */
declare function getValueFromStorage<T>(storageKey: number, propertyName: AllStorageKeys): T | undefined;
/**
 * Retrieves a cursor property from a specific storage
 *
 * @param storageKey The cursor id
 * @param propertyName The property to retrieve
 */
export declare function getValueFromCursorStorage<T>(storageKey: number, propertyName: keyof StorageType["cursor"]): T | undefined;
/**
 * Updates a cursor property from a specific storage
 *
 * @param storageKey The cursor id
 * @param informationToStore An object containing the properties to update. It will append and replace the existing ones for the given storage key and id.
 */
export declare function updateCursorStorage(storageKey: number, informationToStore: StorageType["cursor"]): void;
/**
 * Retrieves a chart property from a specific storage.
 *
 * @param storageKey The chart id
 * @param propertyName The property to retrieve
 */
export declare function getValueFromChartStorage<T>(storageKey: number, propertyName: keyof StorageType["chart"]): T | undefined;
/**
 * Updates a chart property from a specific storage
 *
 * @param storageKey The chart id
 * @param informationToStore An object containing the properties to update. It will append and replace the existing ones for the given storage key and id.
 */
export declare function updateChartStorage(storageKey: number, informationToStore: StorageType["chart"]): void;
/**
 * Retrieves a series property from a specific storage.
 *
 * @param storageKey The series id
 * @param propertyName The property to retrieve
 */
export declare function getValueFromSeriesStorage<T>(storageKey: number, propertyName: keyof StorageType["series"]): T | undefined;
/**
 * Updates a series property from a specific storage
 *
 * @param storageKey The series id
 * @param informationToStore An object containing the properties to update. It will append and replace the existing ones for the given storage key and id.
 */
export declare function updateSeriesStorage(storageKey: number, informationToStore: StorageType["series"]): void;
/**
 * Retrieves an axis property from a specific storage.
 *
 * @param storageKey The axis id
 * @param propertyName The property to retrieve
 */
export declare function getValueFromAxisStorage<T>(storageKey: number, propertyName: keyof StorageType["axis"]): T | undefined;
/**
 * Updates an axis property from a specific storage
 *
 * @param storageKey The axis id
 * @param informationToStore An object containing the properties to update. It will append and replace the existing ones for the given storage key and id.
 */
export declare function updateAxisStorage(storageKey: number, informationToStore: StorageType["axis"]): void;
/**
 * Retrieves a marker property from a specific storage.
 *
 * @param storageKey The marker id
 * @param propertyName The property to retrieve
 */
export declare function getValueFromMarkerStorage<T>(storageKey: number, propertyName: keyof StorageType["marker"]): T | undefined;
/**
 * Updates a marker property from a specific storage
 *
 * @param storageKey The marker id
 * @param informationToStore An object containing the properties to update. It will append and replace the existing ones for the given storage key and id.
 */
export declare function updateMarkerStorage(storageKey: number, informationToStore: StorageType["marker"]): void;
/**
 * Retrieves a label property from a specific storage.
 *
 * @param storageKey The label id
 * @param propertyName The property to retrieve
 */
export declare function getValueFromLabelStorage<T>(storageKey: number, propertyName: keyof StorageType["label"]): T | undefined;
/**
 * Updates a label property from a specific storage
 *
 * @param storageKey The label id
 * @param informationToStore An object containing the properties to update. It will append and replace the existing ones for the given storage key and id.
 */
export declare function updateLabelStorage(storageKey: number, informationToStore: StorageType["label"]): void;
/**
 * Retrieves a guide property from a specific storage.
 *
 * @param storageKey The guide id
 * @param propertyName The property to retrieve
 */
export declare function getValueFromGuideStorage<T>(storageKey: number, propertyName: keyof StorageType["guide"]): T | undefined;
/**
 * Updates a guide property from a specific storage
 *
 * @param storageKey The guide id
 * @param informationToStore An object containing the properties to update. It will append and replace the existing ones for the given storage key and id.
 */
export declare function updateGuideStorage(storageKey: number, informationToStore: StorageType["guide"]): void;
export declare const nextUtilsMemoryStoragePrivateFunctions: {
    updateStorage: typeof updateStorage;
    getValueFromStorage: typeof getValueFromStorage;
};
export {};
