import { Sprite, Label } from '@amcharts/amcharts5';
import { XYSeries, XYChart } from '@amcharts/amcharts5/xy.js';
import { WebChartTypes } from '../../../../../spec';
import { AmChartsColor } from '../../../types';
import { DataLabelFormatter, DataLabelUpdateProps, DefaultDataLabelFormatterProps } from './interfaces';
interface finalizeBarLineAndHeatChartDataLabelsProps extends DataLabelUpdateProps {
    isHeatChart?: boolean;
    dataLabelFormatter: (props: DefaultDataLabelFormatterProps) => DataLabelFormatter;
}
/**
 * determine if label is outside the charting area
 *
 * @param props.label the data label
 * @param props.chart the chart
 * @param props.seriesType the series type
 */
export declare function adjustLabelVisibility(props: {
    label: Sprite;
    chart: XYChart | undefined;
    seriesType: WebChartTypes;
}): void;
/**
 * initialize a series data labels
 *
 * @param props.DataLabelUpdateProps props to pass to initialize series data labels
 */
export declare function initializeBarLineSeriesDataLabel(props: DataLabelUpdateProps): void;
/**
 * removes data labels from a series
 *
 * @param series The chart's series
 * @param index Index of what series bullets should be removed
 */
export declare function removeSeriesDataLabel(series: XYSeries, index?: number): void;
/**
 * Gets the best inverse color to use for the data label text based on the background color of the text.
 * If the chart background color is contrasting enough with the text background color, it will be used as the text color.
 * Otherwise, either black or white will be used based on the lightness of the text background color.
 *
 * @param textBackgroundColor The background color of the data label, it can be the color of the column or the series.
 * @param chartBackgroundColor The background color of the chart.
 * @param fillOpacity The fill opactiy of the background of the data label
 * @returns The best inverse color to use for the data label text
 */
export declare function getContrastTextColor(props: {
    textBackgroundColor: AmChartsColor;
    chartBackgroundColor: AmChartsColor;
    fillOpacity?: number;
}): AmChartsColor;
/**
 * Adds a color adapter to the data label to inverse the text color based on the series color and the chart background color.
 *
 * @param props.dataLabel The data label to add the adapter to
 * @param props.labelConfigColor The color configured for the data label
 * @param props.seriesColor The series color, used as a fallback for the text background color
 * @param props.backgroundColor The chart background color, used as a fallback for the text background color
 * @param props.autoInverseDataLabelTextColor If true, the text color will be inverted based on the series and chart background colors
 */
export declare function addDataLabelColorAdapter(props: {
    dataLabel: Label;
    labelConfigColor?: AmChartsColor;
    seriesColor?: AmChartsColor;
    backgroundColor?: AmChartsColor;
    autoInverseDataLabelTextColor?: boolean;
    isHeatChart?: boolean;
}): void;
/**
 * Generic function to update a series data labels.
 * This function contains common code for all the XY Charts. The specific code for each chart
 * is to find in the `update<chart type>DataLabel()` functions
 *
 * @param props Set of properties necessary to update the data label
 */
export declare function updateDataLabel(props: DataLabelUpdateProps): void;
/**
 * sets a series data labels
 *
 * @param props.DataLabelUpdateProps props to pass to sets series data labels
 */
export declare function setSeriesDataLabel(props: DataLabelUpdateProps): void;
export declare function finalizeBarLineAndHeatChartDataLabels(props: finalizeBarLineAndHeatChartDataLabelsProps): void;
/**
 * Updates a series data labels
 *
 * @param props props to pass to update series data labels
 */
export declare function updateBarLineSeriesDataLabel(props: DataLabelUpdateProps): void;
export {};
