import { Chart, Modal, Root, SerialChart } from '@amcharts/amcharts5';
import { ChartOptions } from '../../../common/interfaces';
import { LoaderHandle } from './interfaces';
/**
 * Initialize the loader and defines the nested functions
 *
 * @param root The chart's root
 * @param chart The chart
 * @param options The chart's options
 * @return The loader handle
 */
export declare function initializeLoader(props: {
    root: Root;
    chart: Chart;
    options?: ChartOptions;
}): LoaderHandle;
/**
 * Applies to the loader colors to the loading curtain, spinner and chart modal.
 * @param props.chart The chart
 * @param props.updateOptions Update options
 * @param props.chartModal The chart's modal
 */
export declare function applyLoaderColors(props: {
    chart: SerialChart;
    updateOptions?: ChartOptions;
    chartModal?: Modal;
}): void;
/**
 * Hides the loader when the chart is done rendering
 *
 * @param series
 * @param hideLoaderHandle
 */
export declare function hideLoaderAfterRender(root: Root, hideLoaderHandle: LoaderHandle["hideLoader"]): void;
