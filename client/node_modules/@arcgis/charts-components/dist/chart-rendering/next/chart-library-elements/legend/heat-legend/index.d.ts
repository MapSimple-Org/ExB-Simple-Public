import { HeatLegend } from '@amcharts/amcharts5';
import { WebChartSeriesType, WebChart } from '../../../../../spec';
import { XYChart } from '@amcharts/amcharts5/xy.js';
/**
 * Initializes the legend
 *
 * @param chart The chart
 */
export declare function initializeHeatLegend(chart: XYChart): HeatLegend;
/**
 * Update the legend
 *
 * @param chart The chart
 * @param chartConfig The chart config
 * @param legend The legend
 * @param hideEmptySeries if we should hide empty series
 * @param seriesConfig The series config
 */
export declare function updateHeatChartLegend(props: {
    chart: XYChart;
    chartConfig: WebChart;
    legend?: HeatLegend;
    hideEmptySeries?: boolean;
    seriesConfig: WebChartSeriesType[];
}): void;
