import { Series, Sprite, Tooltip } from '@amcharts/amcharts5';
import { ColumnSeries } from '@amcharts/amcharts5/xy.js';
import { IField, WebHeatChart, WebChartCalendarDatePartsUnits, WebChartHeatChartSeries } from '../../../../../spec';
import { UtilsStrings } from '../../../../../utils';
import { HeatChartLabelFormatCallback } from '../../../../common/interfaces';
import { HeatChartTooltipProps } from './interfaces';
/**
 * Util method to generate date parts object for tooltip to create it's content
 * @param props.value number that we are checking
 * @param props.config chartConfig config
 * @param props.formatLocale what locale we are formatting too
 * @param props.unit what unit we are looking at
 * @param props.utilsStrings list of util strings for translation
 * @returns DatePartType
 */
export declare function getDateParts(props: {
    value: number;
    config: WebHeatChart;
    formatLocale?: string;
    unit?: WebChartCalendarDatePartsUnits;
    utilsStrings: UtilsStrings;
}): {
    label: string;
    value: string;
};
/**
 * Util method to generate heat chart tooltip content
 * @param props.columnSeries the series for the tooltip
 * @param props.seriesConfig series config
 * @param props.utilsStrings list of util strings for translation
 * @param props.fieldList list of fields
 * @returns string
 */
export declare function generateDefaultHeatChartTooltipContentFormatter(props: {
    columnSeries: Series;
    seriesConfig: WebChartHeatChartSeries;
    utilsStrings: UtilsStrings;
    fieldList?: IField[];
}): HeatChartLabelFormatCallback;
/**
 * Adds the html code to the tooltip associated to the heat chart
 * @param seriesConfig The series config
 * @param heatSeries: Heat sereis
 * @param tooltip the tooltip that we are adding editing
 */
export declare function heatChartTooltipContentCallback(props: {
    seriesConfig: WebChartHeatChartSeries;
    heatSeries: Series;
    tooltip: Sprite | Tooltip;
}): string | undefined;
/**
 * Initializes the tooltip associated to the slices on a heat chart
 * @param columnSeries The series containing the slices on which to add a tooltip text adapter
 */
export declare function initializeHeatChartTooltip(columnSeries: ColumnSeries): void;
/**
 * Handles how the tooltip is rendered on a heat chart.
 * If a format callback is passed, we'll use it over the default tooltip formatting.
 * @param props Necessary information to update the tooltip
 */
export declare function updateHeatChartTooltip(props: HeatChartTooltipProps): void;
