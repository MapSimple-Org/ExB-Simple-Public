import { HeatLegend, Legend, SerialChart, Series } from '@amcharts/amcharts5';
import { WebChart, WebChartSeriesType } from '../../../../spec';
/**
 * Assigns the appropriate label opacity to a chart's legend when there is a chart update
 *
 * @param series The chart series of which legend labels are to be set.
 */
export declare function handleLegendLabelsOpacity(series: Series): void;
/**
 * Sets the legend's data (series or slices to display)
 *
 * @param props.chart The chart
 * @param props.legend The legend
 * @param props.chartConfig The chart configuration
 * @param props.hideEmptySeries Whether to hide the series in the legend if the data item is empty or invalid
 */
export declare function updateLegendData(props: {
    chart: SerialChart;
    legend: Legend;
    chartConfig: WebChart;
    hideEmptySeries?: boolean;
}): void;
/**
 * Initializes the legend
 *
 * @param chart The chart
 */
export declare function initializeLegend(props: {
    chart: SerialChart;
    seriesConfig?: WebChartSeriesType[];
    isHeatChart?: boolean;
}): HeatLegend | Legend;
/**
 * Sets the legend visibility
 *
 * @param props.chart The chart
 * @param props.legend The legend
 * @param props.showLegend Whether to show the legend
 */
export declare function setLegendVisibility(props: {
    chart: SerialChart;
    legend: HeatLegend | Legend;
    showLegend: boolean;
}): void;
/**
 * Updates the legend
 *
 * @param props.chart The chart
 * @param props.chartConfig The chart config
 * @param props.legend The legend
 * @param props.hideEmptySeries Whether to hide the series in the legend if the data item is empty or invalid
 * @param props.seriesConfig The series config
 */
export declare function updateLegend(props: {
    chart: SerialChart;
    chartConfig: WebChart;
    legend?: HeatLegend | Legend;
    hideEmptySeries?: boolean;
    seriesConfig: WebChartSeriesType[];
    isHeatChart?: boolean;
    heatChartLegendUpdatedNeeded?: boolean;
}): void;
