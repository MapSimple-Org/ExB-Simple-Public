import { XYChart } from '@amcharts/amcharts5/xy.js';
import { CategoryFormatOptions, DateTimeFormatOptions, NumberFormatOptions } from '../../../../../../../spec';
import { AxisLabelFormatCallback } from '../../../../../../common/interfaces';
/**
 * Generates the default label formatter for the category axis
 *
 * @param props.chart The chart
 * @param props.formatOptions The format options
 * @param props.isNonAggregatedChart Whether the chart is non-aggregated
 */
export declare function generateDefaultCategoryAxisFormatter(props: {
    chart: XYChart;
    formatOptions: CategoryFormatOptions | DateTimeFormatOptions | NumberFormatOptions;
    isNonAggregatedChart?: boolean;
}): AxisLabelFormatCallback;
/**
 * Generates the default label formatter for the category axis labels for heat charts.
 *
 * @param props.chart The chart
 * @param props.isXAxis Whether the formatter is for the x-axis or y-axis
 * @param props.formatOptions The format options
 */
export declare function generateDefaultCategoryAxisFormatterForHeatChart(props: {
    chart: XYChart;
    isXAxis?: boolean;
    formatOptions: CategoryFormatOptions | DateTimeFormatOptions | NumberFormatOptions;
}): AxisLabelFormatCallback;
