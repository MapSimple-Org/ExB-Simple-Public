import { Container, DataItem, Graphics, Label, Root, Sprite } from '@amcharts/amcharts5';
import { IAxisDataItem } from '@amcharts/amcharts5/xy.js';
import { AmChartsColor, EsriColor } from '../../types';
/**
 * enable and modify the responsive features of the root/chart
 *
 * @param root - amCharts root object
 * @param enableResponsiveFeatures - boolean to enable or disable the responsive features
 */
export declare function activateMobileFeatures(root: Root, enableResponsiveFeatures?: boolean): void;
/**
 * enable and modify the animation features of the root/chart
 *
 * @param root amcharts root object
 * @param enableChartAnimation boolean to enable the chart animation
 */
export declare function enableChartAnimation(root: Root, enableChartAnimation?: boolean): void;
export declare function convertColor(configColor?: EsriColor | Readonly<EsriColor>): {
    color: AmChartsColor;
    opacity: number;
};
/**
 * Toggles the visibility of an element via `.show()`/`.hide()` and `visible` property.
 * The [reason](https://amcharts.zendesk.com/hc/en-us/requests/52191) for using `.show()`/`.hide()` approach.
 * Observing some misbehavior if just `.show()`/`.hide()` is used, so using `visible` along with.
 *
 * @param element
 * @param visible
 */
export declare function showElement(element: DataItem<IAxisDataItem> | Graphics | Sprite, visible?: boolean): void;
/**
 * Helps finding a container's child from its id.
 *
 * @param childId The child's id to find.
 * @param parent The parent container to search in.
 * @param useChildId Optional flag that tells the method to use the childId or search for it (note: used for dual axis charts)
 */
export declare function findContainerChild<T extends Container | Graphics | Label>(props: {
    childId: string;
    parent: Container;
    useChildId?: boolean;
}): T | undefined;
/**
 * Creates a unique element id so we can render multiple charts on a page
 *
 * @param elementId The element id.
 * @param parentUID The parent container id.
 */
export declare function getUniqueElementID(elementId: string, parentUID: number): string;
/**
 * With amCharts v5, there is no layout validated method or knowning when the chart is ready
 * see: https://www.amcharts.com/docs/v5/getting-started/root-element/#chart-ready-event
 * For now, we do similar to amCharts an wait 100ms to do anything with elements positions
 * "Our code waits until there's an idle moment of at least 100ms, before "deciding" the chart is fully functional and initial animations are played out."
 * We return the timeout in case we need to clear it for "frameended"
 */
export declare function runAfterChartIsReady<T>(block: () => T, delay?: number): NodeJS.Timeout;
