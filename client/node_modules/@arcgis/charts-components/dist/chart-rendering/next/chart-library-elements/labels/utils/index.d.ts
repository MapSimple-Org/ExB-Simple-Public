import { Chart, Label } from '@amcharts/amcharts5';
import { LabelType } from '../../../../common/interfaces';
/**
 * Escape the brackets (i.e. [, ], {, } ) contained in a string by doubling them,
 * as per amCharts documentation: https://www.amcharts.com/docs/v4/concepts/formatters/formatting-strings/#Escaping
 *
 * @param value
 */
export declare function escapeBrackets(value: string): string;
/**
 * Adjusts a label max width or height according to either a fixed size or its container's size
 *
 * @param props.chart The chart
 * @param props.label The label to wrap
 * @param props.labelType The type of label, to decide what rule to apply
 */
export declare function autoAdjustLabelDimensionCallback(props: {
    chart: Chart;
    label: Label;
    labelType?: LabelType;
}): void;
/**
 * Allows a label to automatically adjusts its max width or height according to either a fixed size or its container's size
 *
 * @param props.chart The chart
 * @param props.label The label to wrap
 * @param props.labelType The type of label, to decide what rule to apply
 */
export declare function autoAdjustLabelDimension(props: {
    chart: Chart;
    label: Label;
    labelType?: LabelType;
}): void;
/**
 * Gets the formatted time string for a given hour and minute, supporting all Intl.DateTimeFormatOptions.
 *
 * @param props.hour The hour (0-23)
 * @param props.minute The minute (0-59)
 * @param props.format Intl.DateTimeFormatOptions for formatting
 * @param props.locale The locale to use for formatting
 * @returns For a valid input value, the function returns the formatted input string according to the provided format,
 * or a string representation of the hour and minute if the format is not provided; otherwise, it returns the input value as a string.
 */
export declare function getHourOfDayString(props: {
    hour: number;
    minute?: number;
    format?: Intl.DateTimeFormatOptions;
    locale?: string;
}): string;
/**
 * Gets the name of the weekday for a given weekday number, supporting all Intl.DateTimeFormatOptions.
 *
 * @param props weekdayNumber The weekday number (1-7)
 * @param props.format Intl.DateTimeFormatOptions for formatting
 * @param props.locale The locale to use for formatting
 * @param props.firstDayOfWeek The first day of the week (1 = Monday, 7 = Sunday), defaults to 7 (Sunday).
 * @returns For a valid input value, the function returns the formatted input string according to the provided format,
 * or the weekday name (like Monday for example) if the format is not provided; otherwise, it returns the input value as a string.
 */
export declare function getWeekdayName(props: {
    weekdayNumber: number;
    format?: Intl.DateTimeFormatOptions;
    locale?: string;
    firstDayOfWeek?: number;
}): string;
/**
 * Gets the name of the month for a given month number, supporting all Intl.DateTimeFormatOptions.
 *
 * @param props monthNumber The month number (1-12)
 * @param props.format Intl.DateTimeFormatOptions for formatting
 * @param props.locale The locale to use for formatting
 * @returns For a valid input value, the function returns the formatted input string according to the provided format,
 * or the month (like January for example) if the format is not provided; otherwise, it returns the input value as a string.
 */
export declare function getMonthName(props: {
    monthNumber: number;
    format?: Intl.DateTimeFormatOptions;
    locale?: string;
}): string;
