import { PieChart } from '@amcharts/amcharts5/percent.js';
import { UtilsStrings } from '../../../../../../utils';
import { WebChartDataItem, WebChartDataTypes, WebChartPieChartSeries } from '../../../../../../spec';
/**
 * Initialize the pie chart slice group and the chart's data.
 * @param props.chart The pie chart
 * @param props.data The pie chart data
 * @param props.seriesConfig The series config
 * @param props.utilsStrings The t9n strings to fetch the slice group label
 */
export declare function initializePieChartGroupOfSlices(props: {
    chart: PieChart;
    data: WebChartDataItem[];
    seriesConfig: WebChartPieChartSeries[];
    utilsStrings: UtilsStrings;
}): WebChartDataItem[] | undefined;
/**
 * Update the pie chart slice group and the chart's data.
 * @param props.chart The pie chart
 * @param props.data The chart's latest data
 * @param props.seriesConfig The series config
 * @param props.utilsStrings The t9n strings to fetch the slice group label
 */
export declare function updatePieChartGroupOfSlices(props: {
    chart: PieChart;
    data?: WebChartDataTypes;
    seriesConfig: WebChartPieChartSeries[];
    utilsStrings: UtilsStrings;
}): WebChartDataItem[] | undefined;
