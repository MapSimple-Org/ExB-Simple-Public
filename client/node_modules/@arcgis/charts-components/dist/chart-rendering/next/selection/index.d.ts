import { Graphics, SerialChart, Slice, Template } from '@amcharts/amcharts5';
import { PieSeries } from '@amcharts/amcharts5/percent.js';
import { XYCursor, ColumnSeries, LineSeries, XYChart } from '@amcharts/amcharts5/xy.js';
/**
 * Indicates whether a selection has been made by click-and-drag
 * @param cursor The chart's cursor
 */
declare function isSelectionByRange(cursor?: XYCursor): boolean;
/**
 * Adds the selection by click feature to a series
 * @param element The element to add the selection to (bullet, column template...)
 * @param series The chart series
 */
export declare function addSelectionByClick(element: Graphics | Template<Graphics | Slice>, series: ColumnSeries | LineSeries | PieSeries): void;
/**
 * Adds a listener to the xy chart to handle the selection:
 * - by hit, but only to clear the selection. The selection by hit on a an element (bullet...)
 * The selection by click is handled by the functions initializing the elements (e.g. initializeScatterplotMarkers())
 *
 * @param chart The chart
 */
export declare function addClearSelection(chart: SerialChart): void;
/**
 * Adds a listener to the xy chart to handle the selection by range.
 * The selection by click is handled by the functions initializing the elements (e.g. initializeScatterplotMarkers())
 * @param chart The chart
 */
export declare function addRangeSelectionOnXYChart(chart: XYChart): void;
export declare const selectionPrivateFunctions: {
    isSelectionByRange: typeof isSelectionByRange;
};
export {};
