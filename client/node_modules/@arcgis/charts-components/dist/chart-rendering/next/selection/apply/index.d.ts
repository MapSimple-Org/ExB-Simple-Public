import { SerialChart } from '@amcharts/amcharts5';
import { WebChart, WebChartDataTypes, WebGaugeChart, WebChartTypes } from '../../../../spec';
import { ErrorStrings, QueryObject, SelectionSource } from '../../../../utils';
import { ChartOptions } from '../../../common/interfaces';
import { ApplySelectionOnSeriesProps } from './interfaces';
/**
 * Checks whether a selection has been applied to all the series from a chart
 *
 * @param props.chart The chart
 * @param props.chartType The chart type
 * @param props.showMean Whether the mean lines are shown on the box plot
 */
declare function whetherSelectionAppliedToAllTheSeries(props: {
    chart: SerialChart;
    chartType?: WebChartTypes;
    showMean?: boolean;
}): boolean;
/**
 * Apply a selection on a specific series
 *
 * @param props
 */
export declare function applySelectionOnSeries(props: ApplySelectionOnSeriesProps): void;
/**
 * Apply a selection on a chart
 *
 * @param props
 */
export declare function applySelectionOnChart(props: {
    chart: SerialChart;
    chartConfig: WebChart | WebGaugeChart;
    data?: WebChartDataTypes;
    queryObject: QueryObject;
    options?: ChartOptions;
    dataWasUpdated?: boolean;
    sortChanged?: boolean;
    groupOfSlicesWasUpdated?: boolean;
    errorStrings: ErrorStrings;
}): Promise<void>;
/**
 * Finalizes the selection:
 * - gathers the selection items (list of data contexts)
 * - gathers the selection indexes (list of series indexes and sub-indexes)
 * - gathers the selection OIDs
 * - sends the selection event
 *
 * @param chart The chart instance
 * @param selectionSource The source of the selection
 */
export declare function finalizeSelection(chart: SerialChart, selectionSource: SelectionSource): Promise<void>;
export declare const nextSelectionApplyPrivateFunctions: {
    whetherSelectionAppliedToAllTheSeries: typeof whetherSelectionAppliedToAllTheSeries;
};
export {};
