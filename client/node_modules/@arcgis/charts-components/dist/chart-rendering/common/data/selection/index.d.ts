import { WebChart, WebChartDataItem } from '../../../../spec';
import { ChartOptions } from '../../interfaces';
/**
 * Function comparing two array containing selection items.
 *
 * @param props.itemsA First array
 * @param props.itemsB Second array
 * @param props.isPieFromFields Flag to indicate if the chart is a pie chart from fields
 * @param props.x The x-axis field. Currently only used for a specific case related to the heat chart
 * @param props.y The y-axis field. Currently only used for a specific case related to the heat chart
 * @param props.outStatisticFieldName The out statistic field name. Currently only used for a specific case related to the heat chart
 */
export declare function hasSelectionByItemsChanged(props: {
    itemsA?: WebChartDataItem[];
    itemsB?: WebChartDataItem[];
    isPieFromFields?: boolean;
    x?: string;
    y?: string;
    outStatisticFieldName?: string;
}): boolean;
export declare function getSelectedData(props: {
    chartConfig: WebChart;
    currentSelectionDataItems?: WebChartDataItem[];
    options?: ChartOptions;
}): WebChartDataItem[];
