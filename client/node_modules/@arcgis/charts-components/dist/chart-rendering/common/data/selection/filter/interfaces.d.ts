import { RESTFieldType, WebChart, WebChartDataItem } from '../../../../../spec';
import { AbortControllerKeyType } from '../../../../../utils';
import { ChartOptions } from '../../../interfaces';
export declare const States: {
    readonly CustomActive: "custom-active";
    readonly CustomInactive: "custom-inactive";
    readonly Default: "default";
    readonly Active: "active";
};
export interface SelectionProps {
    xAxisField: string;
    yAxisField?: string;
    splitBy?: string;
    chartConfig: WebChart;
}
export interface BuildSelectionFilterProps extends SelectionProps {
    dataContext: WebChartDataItem;
    xAxisFieldType?: RESTFieldType;
    yAxisFieldType?: RESTFieldType;
    outTimeZone?: string;
    dataTimeZone?: string | null;
    runsOnClientQueryEngine?: boolean;
    options?: ChartOptions;
    abortKey?: AbortControllerKeyType;
}
export interface ElementSelectionCommonProps extends SelectionProps {
    elementDataContext?: WebChartDataItem;
    uniqueFeature?: boolean;
    seriesIndex: number;
    elementIndex?: number;
}
