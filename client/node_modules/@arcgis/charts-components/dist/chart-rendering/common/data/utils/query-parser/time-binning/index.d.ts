import { WebChartTemporalBinningUnits } from '../../../../../../spec';
/**
 * Generates the INTERVAL SQL instruction to shift date values from the data time zone to the output time zone.
 * This is inspired by the Dashboards implementation currently found here:
 * https://devtopia.esri.com/WebGIS/arcgis-dashboards/blob/develop-4/packages/dashboard-view-models/addon/-private/service-dataset.js.
 * @param props.outTimeZone The output time zone (typically the user locale)
 * @param props.dataTimeZone The data time zone (usually UTC but can be set differently on the layer)
 * @param props.runsOnClientQueryEngine Whether the query will be executed on the client query engine (FeatureLayerView) or on the server
 */
export declare function getIntervalStatement(props: {
    outTimeZone?: string;
    dataTimeZone?: string | null;
    runsOnClientQueryEngine?: boolean;
}): string;
export declare function getTimeBinGroupByFieldsForStatistics(dateFieldName: string, unit: WebChartTemporalBinningUnits): string[];
