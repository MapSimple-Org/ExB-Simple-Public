import { WebChart, WebChartQuery, WebChartDataTransformations } from '../../../../spec';
import { DataInfo, QueryObject } from '../../../../utils';
import { FetchDataProps } from '../../interfaces';
/**
 * Checks if the current transformation type (log | sqrt) is compatible with the dataset.
 *
 * @param dataTransformationType The transformation requested for the histogram
 * @param min The min value from the data set
 */
declare function isDataCompatibleWithTransformation(dataTransformationType: WebChartDataTransformations | undefined, min: number): boolean;
/**
 * Create the bins for the original min and max value (Before applying the transformation).
 *
 * @param dataTransformationType The transformation requested for the histogram.
 * @param min The min value from the data set.
 */
declare function createInvalidDataInfoForHistogram(dataTransformationType?: WebChartDataTransformations, dataCountIncompatible?: boolean): DataInfo;
/**
 *
 * @param min Work around to reduce the decimal part by 1.
 * @returns number
 */
declare function processMin(min: number): number;
/**
 * Fetches the histogram data and proceed to the statistic calculations.
 * Returns information indicating whether the operation was a success and the data if so.
 *
 * @param props
 */
export declare function fetchFinishedHistogramData(props: FetchDataProps): Promise<DataInfo>;
/**
 * Fetches selection data for histogram.
 * It fetches a list of values from the x field, with a selection filter (`OID IN (...)`)
 *
 * @param props.queryObject The object used to query (layer or FeatureLayerView)
 * @param props.queryConfig The query config
 * @param props.chartConfig The chart config
 * @param props.x The x field name
 */
export declare function fetchHistogramSelectionData(props: {
    queryObject: QueryObject;
    queryConfig: WebChartQuery;
    chartConfig: WebChart;
    x: string;
}): Promise<DataInfo>;
export declare const commonDataHistogramPrivateFunctions: {
    processMin: typeof processMin;
    isDataCompatibleWithTransformation: typeof isDataCompatibleWithTransformation;
    createInvalidDataInfoForHistogram: typeof createInvalidDataInfoForHistogram;
};
export {};
