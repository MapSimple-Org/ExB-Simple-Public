import { CommonStrings } from '../../../../utils';
/**
 * Fetches the translation JSON from common assets.
 * @param locale The locale to fetch, e.g. "messages.en.json"
 * @throws {Error} If the file cannot be loaded
 */
export declare function fetchT9nJson(locale: string): Promise<CommonStrings>;
