import { LoaderColors } from '../../../utils';
/**
 * Computes the colors the chart's modal should use (background and text) from the loader colors when provided,
 * or the chart defaults otherwise.
 * @param loaderColors The loader colors`
 * @returns String values representing the CSS colors (rgba) for the modal background and text.
 */
export declare function getModalColors(loaderColors?: LoaderColors): {
    textCSSColor: string;
    backgroundCSSColor: string;
};
