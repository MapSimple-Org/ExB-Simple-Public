/// <reference path="../../index.d.ts" />
import { default as Layer } from '@arcgis/core/layers/Layer.js';
import { default as UtilityNetwork } from '@arcgis/core/networks/UtilityNetwork.js';
import { default as TraceResult } from '@arcgis/core/rest/networks/support/TraceResult.js';
import { JsxNode, PublicLitElement as LitElement, TargetedEvent } from '@arcgis/lumina';
import { PropertyValues } from 'lit';

type ResultType = "features" | "functions";
/**
 * This is a sub-component used by the **ArcgisUtilityNetworkTraceAnalysis** component.
 *
 * This sub-component takes a `TraceResult` instance as input.
 * Optionally, the title of a trace configuration can be provided.
 *
 * It displays a list of result types returned by a trace in the provided `TraceResult`.
 * Selecting a trace result type renders a flow item for that result type.
 *
 * [Read more...](https://developers.arcgis.com/javascript/latest/references/map-components/arcgis-utility-network-trace-results/)
 *
 * @since 4.34
 * @internal
 */
export declare class ArcgisUtilityNetworkTraceResults extends LitElement {
    layers: Layer[];
    messageOverrides?: typeof this._messages._overrides;
    get selectedResultType(): ResultType | undefined;
    traceConfigTitle?: string;
    traceResult?: TraceResult;
    utilityNetwork?: UtilityNetwork;
    readonly arcgisSelectedResultTypeChange: TargetedEvent<this, void>;
    private _messages;
}
export {};
