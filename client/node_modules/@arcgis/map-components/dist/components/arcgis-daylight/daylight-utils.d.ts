import { default as VirtualLighting } from '@arcgis/core/views/3d/environment/VirtualLighting.js';
import { default as SunLighting } from '@arcgis/core/views/3d/environment/SunLighting.js';
export type DateOrSeason = "date" | "season";
export interface SeasonConfig {
    dayOfMonth: number;
    month: number;
}
export type Hemisphere = "northern" | "southern";
export declare const orderedSeasons: readonly ["spring", "summer", "fall", "winter"];
export type Season = (typeof orderedSeasons)[number];
/**
 * Returns a date object which has the same year and time as the reference, but whose day of the month
 * and month are set according to the specified season.
 */
export declare function getSeasonDate(reference: Date, season: Season, hemisphere: Hemisphere): Date;
/**
 * Gets the astronomical season for the specified date.
 */
export declare function getSeasonFromDate(date: Date, hemisphere: Hemisphere): Season;
/**
 * Converts a season in the specified hemisphere to a northern hemisphere season.
 */
export declare function getNorthernHemisphereSeason(season: Season, hemisphere: Hemisphere): Season;
export declare function getHemisphere(latitude: number): Hemisphere;
export declare const dayInMinutes = 1440;
export declare const dayInMilliseconds = 86400000;
export declare function getSunriseAndSunsetTimes(currentDate: Date, latitude: number, longitude: number, UTCDisplayOffset: number): {
    sunrise: Date;
    sunset: Date;
};
export type LightingUnion = SunLighting | VirtualLighting;
export declare function isSunlighting(lighting: LightingUnion | nullish): lighting is SunLighting;
