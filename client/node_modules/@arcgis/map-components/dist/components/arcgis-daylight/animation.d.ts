import { default as SunLighting } from '@arcgis/core/views/3d/environment/SunLighting';
export declare function animateDaylight({ mode, lighting, getPlaySpeed, onProgress, beforeProgress, }: {
    mode: "day" | "year";
    lighting: SunLighting;
    getPlaySpeed: () => number;
    onProgress?: (prev: SunLighting, next: SunLighting) => void;
    beforeProgress?: () => void;
}): IHandle;
export interface DaylightPlaySpeedConfig {
    day: number;
    night?: number;
    twilight?: number;
}
export declare const baseSpeed: number;
export declare function calculatePlaySpeed(sunrise: Date | nullish, sunset: Date | nullish, currentDate: Date, config: DaylightPlaySpeedConfig): number;
