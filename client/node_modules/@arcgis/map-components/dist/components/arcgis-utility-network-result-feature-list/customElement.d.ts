/// <reference path="../../index.d.ts" />
import { default as Layer } from '@arcgis/core/layers/Layer';
import { default as UtilityNetwork } from '@arcgis/core/networks/UtilityNetwork.js';
import { default as NetworkElement } from '@arcgis/core/rest/networks/support/NetworkElement.js';
import { JsxNode, PublicLitElement as LitElement, TargetedEvent } from '@arcgis/lumina';
import { PropertyValues } from 'lit';

type State = "loading" | "ready";
/**
 * This is a sub-component used by the **ArcgisUtilityNetworkTraceAnalysis** component.
 *
 * This sub-component takes trace result elements as input.
 * It displays the layers the elements are from, with a count of elements on each layer.
 * A `UtilityNetwork` and list of layers is required to display feature layer names.
 *
 * [Read more...](https://developers.arcgis.com/javascript/latest/references/map-components/arcgis-utility-network-result-feature-list/)
 *
 * @since 4.34
 * @internal
 */
export declare class ArcgisUtilityNetworkResultFeatureList extends LitElement {
    /** @required */
    elements: NetworkElement[];
    /** @required */
    layers: Layer[];
    messageOverrides?: typeof this._messages._overrides;
    get selectedLayer(): Layer | undefined;
    /** @required */
    utilityNetwork?: UtilityNetwork;
    readonly arcgisSelectedLayerChange: TargetedEvent<this, void>;
    private _messages;
}
export {};
