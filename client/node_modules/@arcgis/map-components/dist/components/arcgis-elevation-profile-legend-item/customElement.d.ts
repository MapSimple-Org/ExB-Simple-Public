/// <reference types="@arcgis/core/interfaces.d.ts" />
/// <reference path="../../index.d.ts" />
import { default as ElevationProfileResult } from '@arcgis/core/views/analysis/ElevationProfile/ElevationProfileResult';
import { JsxNode, PublicLitElement as LitElement, TargetedEvent } from '@arcgis/lumina';
import { ElevationProfileEffectiveDisplayUnits, ElevationProfileMessages } from '../arcgis-elevation-profile-next/interfaces';

/**
 * Component which displays statistics for a profile line and a checkbox to toggle its visibility in the chart and view.
 *
 * @private
 */
export declare class ArcgisElevationProfileLegendItem extends LitElement {
    effectiveDisplayUnits?: ElevationProfileEffectiveDisplayUnits;
    /** @default false */
    expanded: boolean;
    /** @default false */
    featureSelected: boolean;
    /** @default false */
    hideCheckbox: boolean;
    messages: ElevationProfileMessages;
    result: ElevationProfileResult;
    destroy(): Promise<void>;
    readonly arcgisExpandedToggle: TargetedEvent<this, __esri.ElevationProfileLineGround | __esri.ElevationProfileLineInput | __esri.ElevationProfileLineQuery | __esri.ElevationProfileLineScene>;
}
