import { default as WebMap } from '@arcgis/core/WebMap.js';
import { default as WebScene } from '@arcgis/core/WebScene.js';
import { default as WebLinkChart } from '@arcgis/core/WebLinkChart';
import { default as Map } from '@arcgis/core/Map.js';
import { default as Basemap } from '@arcgis/core/Basemap.js';
import { default as Ground } from '@arcgis/core/Ground.js';
import { default as Layer } from '@arcgis/core/layers/Layer.js';
import { EventEmitter } from '@arcgis/lumina';
/** A potential source of load errors. */
export type LoadErrorSource = Basemap | Ground | Layer | WebLinkChart | WebMap | WebScene;
/**
 * Watches for load errors on components that have a `map` property. Returns an event that will be
 * emitted each time a load error is thrown. It will also update the component's `loadErrorSources`
 * property with the latest errors.
 *
 * @example
 *
 * ```js
 * class MyMapComponent extends LitElement {
 *   arcgisLoadError = useArcgisLoadError(this);
 * }
 * ```
 */
export declare const useArcgisLoadError: (component: {
    map: Map | nullish;
    loadErrorSources?: LoadErrorSource[];
} & import('@arcgis/lumina').LitElement) => EventEmitter;
