/// <reference types="@arcgis/core/interfaces.d.ts" />
import { GenericController } from '@arcgis/lumina/controllers';
import { nil } from '@arcgis/toolkit/type';
import { LitElement } from '@arcgis/lumina';
interface ScreenLocationComponent extends LitElement {
    view?: __esri.MapView | __esri.SceneView | nil;
    location?: __esri.Point | nil;
    trackLocation: boolean;
}
/**
 * Controller for managing and projecting a map point to a map screen location
 *
 * This class observes changes to a MapView/SceneView, location Point, and tracking state, and projects Point coordinates to screen coordinates. It updates the screen location whenever relevant properties change, and exposes the projected screen point.
 */
export declare class ScreenLocation extends GenericController<__esri.ScreenPoint | nil, ScreenLocationComponent> {
    constructor(component: LitElement & ScreenLocationComponent);
    hostLoaded(): void;
    private _updateScreenLocation;
}
export declare const trackScreenLocation: (component: LitElement & ScreenLocationComponent) => nil | __esri.ScreenPoint;
export {};
