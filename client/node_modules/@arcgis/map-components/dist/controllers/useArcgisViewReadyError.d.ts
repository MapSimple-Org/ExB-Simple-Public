import { default as View } from '@arcgis/core/views/View.js';
import { EventEmitter } from '@arcgis/lumina';
/**
 * Watches for view ready state errors on components. Returns an event that will be emitted each
 * time the view's `readyState` indicates an error.
 *
 * @example
 *
 * ```js
 * class MyMapComponent extends LitElement {
 *   arcgisViewReadyError = useArcgisViewReadyError(this);
 * }
 * ```
 */
export declare const useArcgisViewReadyError: (component: {
    view: nullish | View;
} & import('@arcgis/lumina').LitElement) => EventEmitter;
