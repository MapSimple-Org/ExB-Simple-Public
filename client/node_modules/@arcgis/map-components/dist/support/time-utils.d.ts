export type TimeUnit = "centuries" | "days" | "decades" | "hours" | "milliseconds" | "minutes" | "months" | "seconds" | "weeks" | "years";
export type IANATimeZone = NonNullable<Intl.DateTimeFormatOptions["timeZone"]>;
type SystemTimeZone = "system";
type UnknownTimeZone = "unknown";
export type TimeZone = IANATimeZone | SystemTimeZone | UnknownTimeZone;
export declare const system: TimeZone;
export declare const unknown: TimeZone;
export declare const utc: TimeZone;
export declare const systemTimeZone: string;
export declare const millisecondsPerTimeUnit: Record<TimeUnit, number>;
export declare function convertTime(value: number, srcUnit: TimeUnit, dstUnit: TimeUnit): number;
export declare function timeSinceUTCMidnight(utcDate: Date, unit?: TimeUnit): number;
/**
 * Returns a date object which has the same date as the reference, but the given
 * time since UTC midnight.
 */
export declare function makeUTCTime(reference: Date, timeSinceUTCMidnight: number, unit?: TimeUnit): Date;
/**
 * Returns a local date that has the same day/month as the given UTC date, but
 * no time (i.e. midnight).
 */
export declare function truncateLocalTime(date: Date): Date;
/**
 * Returns a UTC datetime that has the same day/month as the given local date,
 * and the same time as the given UTC reference.
 */
export declare function resetUTCDate(utcReference: Date, localDate: Date): Date;
/**
 * Gets whether the specified value is a valid date.
 */
export declare function isValidDate(d: unknown): d is Date;
export declare function offsetDate(date: Date, hours: number, unit?: TimeUnit): Date;
export declare function utcToLocalParts(utcDate: Date, offsetHours: number): {
    localDate: string;
    minutesSinceMidnight: number;
};
export {};
