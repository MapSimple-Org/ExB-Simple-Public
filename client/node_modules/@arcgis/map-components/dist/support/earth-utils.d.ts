import { default as Point } from '@arcgis/core/geometry/Point';
export declare function longitudeToTimezone(longitude: number, fractional: boolean): number;
export interface TimezoneInfo {
    hours: number;
    minutes: number;
    seconds: number;
}
export declare function positionToTimezoneInfo(lonLat: (nullish | number)[] | nullish, ret?: TimezoneInfo | null): nullish | TimezoneInfo;
export declare function timezoneFromCamera(cameraPosition: Point): number;
