/**
 * Computes the mathematical (Euclidean) modulo of `n` with respect to `m`.
 * Unlike JavaScript's `%` operator, this always returns a non-negative result
 * in the range [0, m).
 */
export declare function modulo(n: number, m: number): number;
