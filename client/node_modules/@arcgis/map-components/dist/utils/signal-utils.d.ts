type EqualityFunction<T> = (a: T, b: T) => boolean;
export interface Computed<T> {
    get value(): T;
}
export declare function equalsShallow(a: unknown, b: unknown): boolean;
/**
 * Creates a reactive readonly value which can be used in auto-tracking contexts, just like an `Accessor` property.
 *
 * @method computed
 * @instance
 * @memberof module:esri/core/signal
 *
 * @param {module:esri/core/signal~ComputedExpression} compute
 * @param {module:esri/core/reactiveUtils~ReactiveEqualityFunction} [equalityFunction]
 *
 * @return {module:esri/core/signal~Computed}
 */
export declare function computed<T>(compute: () => T, equalityFunction?: EqualityFunction<T>): Computed<T>;
declare class WritableComputed<T> {
    #private;
    constructor(query: (prev?: T) => T);
    mutate(optimisticValue: T): void;
    get value(): T;
}
export declare function writeableComputed<T>(query: (prev?: nullish | T) => T): WritableComputed<T>;
export {};
