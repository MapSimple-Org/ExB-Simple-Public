import { Root } from '@amcharts/amcharts5/index.js';
/**
 * Returns a new `Root` instance with the locale values loaded and set.
 * @param id
 */
export declare function createRoot(id: HTMLElement | string, locale?: string): Promise<Root>;
