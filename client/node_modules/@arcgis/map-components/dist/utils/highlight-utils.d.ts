/// <reference types="@arcgis/core/interfaces.d.ts" />
import { default as Graphic } from '@arcgis/core/Graphic';
import { default as MapView } from '@arcgis/core/views/MapView';
import { default as SceneView } from '@arcgis/core/views/SceneView';
import { ArcgisMap } from '../components/arcgis-map/customElement.js';
import { ArcgisScene } from '../components/arcgis-scene/customElement.js';
import { AbortOptions } from './async-utils';
type ViewOrViewElement = ArcgisMap | ArcgisScene | MapView | SceneView;
/**
 * Highlights a given feature on the provided view or view element and returns a handle to remove the highlight.
 *
 * It attempts to retrieve the layer view for the provided feature's layer, checks for an available highlight method,
 * and applies the highlight if possible. If highlighting is not supported, it returns a no-op handle.
 *
 * @param view - The view or view element where the feature should be highlighted.
 * @param feature - The feature to highlight.
 * @param options - Optional abort options to cancel the operation if needed.
 *
 * @returns A promise that resolves to a handle that can be used to remove the highlight.
 */
export declare function highlightFeature(view: ViewOrViewElement, feature: Graphic, options?: AbortOptions): Promise<__esri.Handle>;
export {};
