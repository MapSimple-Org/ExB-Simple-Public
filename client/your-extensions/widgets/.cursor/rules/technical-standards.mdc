---
description: Hard technical standards for MapSimple coding
globs: **/*.{ts,tsx,js}
alwaysApply: true
---
# Technical Standards
- **Versioning**: Increment `MINOR_VERSION` in `version.ts` for every change.
- **Logging**: NEVER use `console.log`. Use `debugLogger`.
- **Performance**: Follow the SQL Optimizer and SARGable query patterns documented in `@DEVELOPMENT_GUIDE.md` Section 8.
